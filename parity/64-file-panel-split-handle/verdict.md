# 64 — verdict (review cycle 1)
VERDICT: MATCH
h10 без pr, грип-рецепт, гейт — 1:1 (ratio-механика — см. 65).

## Цикл 5: MATCH

Сплит файловой панели: h10 без `pr`, грип 1:1.

## Цикл 6: MATCH

Сплит 1:1.

## Цикл 16: DIVERGES

Драг сплита файловой панели меняет ratio; оригинал считает пиксели `max(100, startHeight − deltaY)`.

## Цикл 19: DIVERGES

Минимум нижней карты: оригинал жёстко 100 px, у нас `BOTTOM_RATIO_MIN 0.1` от body_h = 83.4 px; база доли — body_h вместо `innerHeight`.

## Цикл 19 (доработка): DIVERGES

Закрыто: драг ведёт ПИКСЕЛЯМИ с жёстким минимумом 100 (`max(100, startHeight − deltaY)`), ratio считается от высоты ОКНА и используется только для персиста.
Ждёт подтверждения замером.

## Цикл 23: MATCH

Драг сплита считает пиксели: `clamp(round(init·vh) − round(dy), 100, body_h−100)`, ratio только для персиста.
