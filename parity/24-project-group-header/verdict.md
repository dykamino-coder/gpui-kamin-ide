
## Цикл 3: DIVERGES

Chevron группы был без бокса (13px глиф вместо width 16 + text-align center) → сдвиг ~2 CSS-px у всей строки. Исправлено волной 6.

## Цикл 4: MATCH

Заголовок группы: chevron 13 в боксе 16 по центру, h26, gap 6, pl6/pr4, badge min-w16/h16/px5/r9/bg-surface/fs-xs — совпало в пределах 1 лог. px. «…» у обрезанного имени нет — ограничение `truncate()`.

## Цикл 8: MATCH

Заголовок группы: полоса 152.0..168.0 против 152.8..168.8; h26, chevron 13 в боксе 16, badge min-w16/h16/px5/r9.

## Цикл 13: DIVERGES

Закрыто: у хедера группы появился тултип с путём папки, а у проекта без
папки — «Sessions without a folder» (`ProjectGroup.tsx:49`).

Осталось: RMB по хедеру открывает модалку удаления проекта — этого в
оригинале нет (наша добавка, не удаляю без решения пользователя).

## Цикл 14: DIVERGES

СНЯТА МОЯ ОШИБКА: в тултип уходил `project.id` — а это UUID из хоста, не
путь. Теперь передаётся `folder_path`, и ветка «Sessions without a folder»
наконец достижима (раньше id был UUID и у проекта без папки).

Закрыто: шеврон 13 → **16** (класс на самом `.codicon`).

Осталось: RMB по хедеру открывает модалку удаления проекта — в оригинале на
группе RMB не обработан.

## Цикл 16: DIVERGES

Осталось: (1) `text_ellipsis()` не рисует «…» у имени папки — нужен `ui::text_fit::fit` (общий системный остаток, см. FIXLOG ц.16); (2) тултип висит на всей строке 26 px, у оригинала — только на `.name`; (3) RMB по заголовку открывает наш «Delete project» — интеракции сверх оригинала, оставлена намеренно.

## Цикл 20: DIVERGES

Закрыто: тултип теперь только на имени группы.
Осталось: «…» при усечении (`ui::text_fit::fit`) и наш RMB «Delete project» — интеракция сверх оригинала, оставлена намеренно.

## Цикл 22 (правка): DIVERGES

Закрыто: «…» при усечении. Многоточие в порте дописывает `text_fit`, движок его не рисует; добавлен `fit_approx` (оценка по числу символов, ±1 символ) и бюджеты по ширине контейнера из probe прошлого кадра. Проверено кадром: «Create new docx d…».

## Цикл 23: DIVERGES

Код «…» на месте (`fit_approx`, бюджет `sidebar_w − 84`), но бюджет завышен на 8.8 лог.: реальный бокс имени 120.0 против 128.8 в коде — имя такой ширины пройдёт проверку и будет срезано движком без «…».

## Цикл 23: MATCH

Закрыто в этом цикле: бюджет усечения имени группы приведён к замеру кадра — вычет 92.8 вместо 84 (реальный бокс имени 120.0 при сайдбаре 212.8). Имя шириной 120…128.8 больше не проходит проверку молча.
