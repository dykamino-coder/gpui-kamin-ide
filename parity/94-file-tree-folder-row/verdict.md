

## Цикл 4: DIVERGES

Метрики строки папки приведены (h 22 + резерв бордера, chevron 13 в боксе 16,
иконка 16, gap 6) — **волна 6**; ховер по ВЫДЕЛЕННОЙ строке гасил
accent-градиент — **исправлено волной 7** (`.when(!is_selected)`).
Остаётся: спиннер не вращается (нет анимации `codicon-modifier-spin`),
`.dropTarget` (accent 22% + outline −1) нет, папки не draggable,
нет Shift-select/клавиатуры/`.flash`.

## Цикл 8: DIVERGES

Метрики и ховер над выделенной закрыты, шаг строк 22.0 у обеих сторон. Осталось: спиннер не вращается, `.dropTarget` нет, папки не draggable, нет Shift-select/клавиатуры/`.flash`.

## Цикл 10: DIVERGES

Метрики совпали живьём: шаг строк 22.08 (оригинал 22), индент 12.04, gap 6, бокс chevron 16, иконка 16. ОСТАЛОСЬ: спиннер без вращения (codicon-modifier-spin); .dropTarget не реализован (grep = 0); папки не draggable; нет shift-select, клавиатуры и .flash.

## Цикл 12: DIVERGES

Закрыто: у лейбла строки появился тултип с полным путём (`data-tooltip={path}`) —
усечённое эллипсисом имя иначе было нечем прочитать.

Осталось: `.dropTarget`; папки не draggable; Shift-select; клавиатурная навигация;
`.flash`; вращение спиннера (нет `transform` в gpui).

## Цикл 13 (ревью зоны): DIVERGES

Закрыто: шеврон 13 → **16**; строке возвращены `white-space: nowrap` +
`overflow: hidden` (были только у лейбла, длинный бейдж вылезал за строку).

Осталось: `.dropTarget`; папки не draggable (`draggable={depth > 0}`);
Shift-диапазон и клавиатура (Delete/F2/Ctrl+X/C/V); `.flash`; тултип лейбла =
`deco?.tooltip ?? path`.

## Цикл 15: DIVERGES

Осталось: drag папок + `.dropTarget`, вращение спиннера, `.flash` после Locate, «…» при усечении, Shift-диапазон и клавиатура строки.

## Цикл 18: DIVERGES

Замеры совпали (шаг строк 22.0, индент 12.0, gap 6, боксы 16).
Осталось: `.dropTarget`, драг папок и OS-drag, Shift-диапазон, клавиатура строки (Delete/F2/Ctrl+X/C/V), `.flash`, вращение спиннера и «…» при усечении.

## Цикл 21: DIVERGES

Закрыто: ховер строки красит лейбл (замер ц.21: ink не менялся — `.hover().text_color()` до вложенного не доходит).
Осталось: `.dropTarget`, драг папок, Shift-диапазон, клавиатура строки, вращение спиннера, `.flash`, «…».

## Цикл 22 (правка): DIVERGES

Закрыто: «…» при усечении. Многоточие в порте дописывает `text_fit`, движок его не рисует; добавлен `fit_approx` (оценка по числу символов, ±1 символ) и бюджеты по ширине контейнера из probe прошлого кадра. Проверено кадром: «Create new docx d…».

## Цикл 22 (правка): DIVERGES

Закрыто: `.dropTarget` — папка под перетаскиваемым файлом подсвечивается
accent 22 % + рамка accent (`drag_over::<DraggedFile>`), раньше состояния
не было вовсе.

Осталось: OS-drag (`beginNativeDrag`) с мультивыделением, Shift-диапазон,
клавиатура строки (Delete/F2/Ctrl+X/C/V), вращение спиннера.

## Цикл 22 (правка 2): DIVERGES

Закрыто: клавиатура строк — Delete (модалка «Move to Recycle Bin?», для
мультивыделения `DeleteFsMany`), F2 (prompt-переименование), Ctrl+X / Ctrl+C /
Ctrl+V по выделению. В gpui у плоских div-ов фокуса нет, поэтому обработчик
висит на общем key-handler окна и работает по `tree.selected`; при открытой
модалке, палитре, QuickOpen или переименовании клавиши дереву не достаются.

Осталось: OS-drag (`beginNativeDrag`) с мультивыделением, Shift-диапазон.

## Цикл 23: DIVERGES

Закрыто: `.dropTarget`, клавиатура строк, «…». Осталось: папки не таскаются (`on_drag` только у файлов, оригинал `draggable={depth > 0}`); Shift-диапазон не портирован; клик на `mouse-down`, а не `on_click` — начало драга раскрывает папку; спиннер строки статичен, хотя `ui::icon::spinner` уже есть.

## Цикл 23: DIVERGES

Закрыто в этом цикле: строка активируется по `on_click`, а не `mouse-down` (начало драга больше не раскрывает папку), и папки таскаются наравне с файлами (`draggable={depth > 0}`). Осталось: Shift-диапазон и статичный спиннер строки.

## Цикл 23: MATCH

Закрыто: Shift-диапазон — `visible_order()` повторяет обход `rows()` (включая кап уровня), а `applyClickSelection` воспроизведён целиком: Ctrl тоглит и ставит якорь, Shift берёт отрезок между якорем и кликом, обычный клик — одиночное выделение и новый якорь. Проверено живьём: `.antigravity` + Shift по `.cargo` → пять подсвеченных строк подряд.
