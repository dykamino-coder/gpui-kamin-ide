# 46 activity-context-submenu — вердикт ц.15

Расхождение: сабменю ставилось верхом на верх строки «Move to», а
`side: "right"` центрирует его по строке: `top = row.top + row.h/2 − sub.h/2`
(на пяти пунктах — ~71 px).

Правка: центровка по строке-якорю с прежним клампом по вьюпорту.

Статус: правка на месте, **ждёт пиксельного подтверждения**.

## Цикл 15: DIVERGES

## Цикл 17: DIVERGES

Закрыто: высота строки САБМЕНЮ 30 (иконка `PanelIcon` 14×12, не codicon 16) — центровка промахивалась на 4.8 px; левый край = `x + MENU_W − 1` (был +4 лишних).
Ждёт пиксельного подтверждения.

## Цикл 21: DIVERGES

Нормальный случай замерен и совпал (left = `x + MENU_W − 1`, top по центру строки, бокс 180 × 164, строка 30.4).
Закрыто: флип ВЛЕВО, когда справа не влезает — раньше сабменю накрывало родительское меню.

## Цикл 23: DIVERGES

Флип влево есть и условие совпадает, но координата на 5 px левее: оригинал якорит по строке «Move to» (`x + 1 − w`), у нас `x − 4 − w`; на те же 5 px смещён порог флипа.

## Цикл 23: MATCH

Закрыто в этом цикле: якорем флипа стала СТРОКА «Move to» (`a.left = menu.left + 1 + 4`, offset 4) → `x + 1 − w` вместо `x − 4 − w`; на те же 5 px сместился и порог флипа.
