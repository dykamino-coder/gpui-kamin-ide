# 18 — verdict (review cycle 1)
VERDICT: DIVERGES
(deviation: pl48, «N v»). dropBar: полоса 2×22 r1 accent+glow vs border_l чипа;
newTab: цвет secondary vs muted, ml4+pr12=16 vs 6, hover alpha .36 vs НЕПРОЗРАЧНЫЙ
микс accent36%+surface, нет scale (deviation); спейсер без min-w24;
плюс-меню: нет иконок folder-opened/circle-large-outline 14+gap8, item 4/12 vs 6/8,
labels другие, hover 8% vs 10%, нет shadow-dropdown, w210 vs min-w200.

## Цикл 2: MATCH
(deviations: N-кнопка, scale)

## Цикл 8: DIVERGES

Стрип чипов: рецепты strip/dropBar/spacer/newTab/«+»-меню 1:1 (кружок 26×26 #3d3f51, лейблы дословны). Расхождение — зазор чип→«+» 18.00 против 6.40 (см. 04). Отклонения: STRIP_PL 48, «N ⌄» вместо скролла стрипа, нет `scale(1.06)`.

### Правка волны 16 (вердикт не выставлен — ждёт цикла сверки)

Зазор чип→«+» закрыт вместе с 04 (кнопка внутри стрипа перед спейсером, весь зазор = её `margin 0 6`). Отклонения прежние: `STRIP_PL 48`, «N ⌄» вместо скролла стрипа, нет `scale(1.06)`.

## Цикл 9: DIVERGES

Зазор чип→«+» ЗАКРЫТ (6.40 = оригинал, см. 04). Остаются отклонения: `STRIP_PL 48`; «N ⌄» вместо `.strip{overflow-x:auto}`; нет `transform:scale(1.06)` у `.newTab:hover` — в gpui нет transform. НОВОЕ: у нас стрип `flex_shrink()` без `flex_1`, у оригинала `.strip{flex:1}` — стрип не забирает свободное место, поэтому даже с `overflow-x:auto` скроллиться будет нечему.

## Цикл 11: DIVERGES

STRIP_PL 48; «N ⌄» вместо .strip{overflow-x:auto} (у оригинала оверфлоу-кнопки нет вовсе); нет scale(1.06); стрип flex_shrink() без flex_1. Арифметика бюджета ПРОВЕРЕНА и сходится: «N ⌄» с 16 чипов, клипа до порога нет, разница ширины чипа против оригинала ровно 9.6 = STRIP_PL/5.

## Цикл 13: DIVERGES

Закрыто по ревью: (1) глиф второго пункта «+»-пикера — `codicon-circle-large-
outline` `\u{ebb5}`, стоял `circle-outline` `\u{eabc}`; (2) пикер якорится к
ПРЯМОУГОЛЬНИКУ кнопки «+» (`left = btn.left`, `top = btn.bottom + 4`), а не к
координатам курсора — регион `new-session` для этого уже был, но не
использовался.

Проверено на живом окне: меню встаёт под кнопкой, глиф крупного кружка.

Осталось (согласованные отклонения): «N ⌄» вместо горизонтального скролла
чипов; `STRIP_PL = 48`; `transform: scale(1.06)` у «+» невозможен.

## Цикл 13: DIVERGES

Закрыто по ревью: (1) глиф второго пункта «+»-пикера — `codicon-circle-large-
outline` `\u{ebb5}`, стоял `circle-outline` `\u{eabc}`; (2) пикер якорится к
ПРЯМОУГОЛЬНИКУ кнопки «+» (`left = btn.left`, `top = btn.bottom + 4`), а не к
координатам курсора — регион `new-session` для этого уже был, но не
использовался.

Проверено на живом окне: меню встаёт под кнопкой, глиф крупного кружка.

Осталось (согласованные отклонения): «N ⌄» вместо горизонтального скролла
чипов; `STRIP_PL = 48`; `transform: scale(1.06)` у «+» невозможен.

## Цикл 13 (проверка): DIVERGES

Кадр живого окна подтвердил обе правки: «+»-пикер встаёт под кнопкой и рисует
`circle-large-outline`.

## Цикл 15: DIVERGES

Рецепты `.dropBar`/`.spacer`/`.newTab`/`.picker` 1:1. Отклонения СОГЛАСОВАНЫ: `STRIP_PL 48` и кнопка «N ⌄» вместо горизонтального скролла. `transform: scale(1.06)` — упор в gpui.

## Цикл 19: DIVERGES

Рецепты «+», спейсера и пикера подтверждены живьём.
Согласованные отклонения: `STRIP_PL 48` и кнопка «N ⌄». Осталось: `transform: scale(1.06)` (упор gpui) и структура (у оригинала «+»/спейсер/пикер — дети `.strip` с `flex: 1`).

## Цикл 23: DIVERGES

Осталось: `transform: scale(1.06)` у «+» (в gpui поворот и масштаб есть только у `svg()`), и «+»/спейсер — сиблинги стрипа, а не его дети. Цвет ховера закрыт: фон `#58698e` = accent 36 % над bg-surface, глиф `#89b4fa`. В этом цикле добавлено фокус-кольцо.

## Цикл 26: DIVERGES

Осталось: `.strip { flex: 1 }` не воспроизведён, у нас `flex_shrink` без
`flex_1` и компенсация спейсером-сиблингом; при нуле сессий оригинал
возвращает `null` целиком, а у нас «+» виден всегда; `STRIP_PL = 48` вместо
`.tabsSlot { padding: 0 12 }` и кнопка «N ⌄» вместо `overflow-x: auto` —
согласованные отклонения, вернул их в список после тихого выпадения;
`transform: scale(1.06)` — упор движка; `ours.md` разошёлся с кодом по «+»


## Цикл 35: DIVERGES

Закрыто три пункта из пяти.

1. `.strip { flex: 1; min-width: 0 }` воспроизведён буквально: стрип больше не
   держится `flex_shrink` с компенсацией сиблингом-спейсером. «+» и спейсер
   переехали ВНУТРЬ стрипа — в `SessionTabs.tsx:120-134` они его дети, а не
   соседи слота.
2. При НУЛЕ открытых чипов стрип не рисуется целиком: ни чипов, ни «+», ни
   спейсера (`SessionTabs.tsx:98` возвращает `null`). Раньше «+» висел всегда.
   Параметр титлбара стал `Option<AnyElement>`, решение принимается по
   `ordered_chips(...).is_empty()`.
3. `STRIP_PL = 48` удалён. Эта константа подменяла отсутствующую ширину
   `.leftCluster` (досье 02); с настоящей обёрткой отступ удвоился бы. Замер:
   стрип начинается на 216.8 = кластер 204.8 + `padding: 0 12` слота.

`ours.md` приведён к коду (в нём ещё стоял `pl 48` и «+» как сосед слота).

Осталось: «N ⌄» вместо `overflow-x: auto` — сознательное отклонение (у
оригинала лишние чипы прокручиваются, у нас уходят в кнопку с поповером),
жду решения, менять ли; `transform: scale(1.06)` у «+» — упор движка
