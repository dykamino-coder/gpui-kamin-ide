# 38 activity-bar-nav — вердикт ц.15

Расхождения: (1) `data-activity-drop="blocked"` не рисовался вовсе — в бар
всегда уходил `blocked = false`; (2) тинт покрывал бокс 52, а не `<nav>` 48.

Правка: в `activity_bar` добавлен параметр `drop_blocked`
(`RootView::drop_blocked(slot)` — тот же предикат, что у карточной подсветки:
тул тащат из другого слота и он здесь уже пришпилен); подсветка вынесена в
абсолютный бокс шириной `ACTIVITY_BAR_WIDTH`.

Осталось: подсветка гаснет при самодропе на своё место (оригинал держит `over`,
пока курсор над полосой); `transition` цвета — упор в gpui.

Статус: **DIVERGES** (2 пункта закрыты, 1 остаётся).

## Цикл 15: DIVERGES

## Цикл 17: MATCH

Тинт замерен ровно 48 (`ACTIVITY_BAR_WIDTH`), blocked-заливка accent-red 12 % + кольцо 60 %, py 12 / gap 8 / `.list` gap 2. Гашение `over` при самодропе подтверждено и у оригинала (`activity-dnd.ts:69-72`) — не расхождение.

## Цикл 21: DIVERGES

Закрыто: подсветка цели при самодропе снята обратно — источник `activity-dnd.ts:69-71` обнуляет `overSlot`, значит `isOver` там false. Правка ц.18 опиралась на `useActivityDropTarget.ts:36` и была неверной.
Тинт `<nav>` замерен: 4.0…52.0 = ровно 48, accent 10 % + кольцо accent 60 % с инсетом 2.

## Цикл 23: MATCH

Самодроп гасит цель (`idx == from || idx == from+1` → None), тинт = абсолютный бокс шириной бара, заливка accent 10 % + пунктир 60 % с инсетом 2 — дословно `global.css:53-57`.
