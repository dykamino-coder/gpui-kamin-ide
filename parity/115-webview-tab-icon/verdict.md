# 115 — verdict (review cycle 1)
VERDICT: DIVERGES
Элемент отсутствует: webview:// в editor_tabs нет, иконки расширения не резолвятся.

## Цикл 2: DIVERGES
webview://-табы не реализованы.

## Цикл 5: DIVERGES

Иконка таба вебвью отсутствует: `grep "webview://"` по shell пуст, иконка расширения не резолвится, fallback codicon-browser нет.

## Цикл 6: DIVERGES

Иконка таба вебвью не реализована.

## Цикл 7: DIVERGES

WebviewTabIcon не портирован; вебвью-табов нет вовсе, всегда иконка файла из темы.

## Цикл 15: DIVERGES

`WebviewTabIcon` (иконка расширения-владельца с кешем) не портирован — таб рисует файловую иконку.

## Цикл 20: DIVERGES

`WebviewTabIcon` не портирован: таб вебвью рисует файловую иконку и basename.

## Цикл 22: DIVERGES

Без изменений: `WebviewTabIcon` требует вебвью-панелей как вкладок редактора
(`createWebviewPanel`), которых в порте пока нет. Зависит от того же куска,
что и остаток 114.

## Цикл 23: DIVERGES

`createWebviewPanel` в порте не реализован — таб-иконке расширения взяться неоткуда.

## Цикл 26: DIVERGES

Осталось: элемент отсутствует целиком и требует подсистемного шага 114;
уточнение ревью — резолв иконки расширения в порте УЖЕ есть
(`kamin:extensions:icon` плюс кеш, используется панелью расширений), не хватает
только самих вебвью-табов
