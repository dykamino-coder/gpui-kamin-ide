# 19 — verdict (review cycle 1)
VERDICT: DIVERGES
Нет постоянного border transparent у неактивных (сдвиг 1px); фикс w180 vs flex 0 1 180
min44 max240 (следствие deviation); first-child ml2 vs 6; close 16×16/глиф12/hover12%
vs 18×18/10/14%; pin r3 vs 4; sleeping label не muted; switching = спиннер vs
анимированная точка; нет dndDragging .4; light-альфы не портированы.
Ядро (h28, 0 6 0 10, gap6, r12, градиент active 26-14+45%, tinted, dot/pin-свап,
тултипы) — 1:1.

## Цикл 2: DIVERGES
Нет dndDragging .4; потерян margin-right 1px (зазор 2 vs 3).

## Цикл 8: MATCH

Чип сессии совпал целиком: h28, радиус-профиль, ml6/2 + mr1 (шаг 3.20), градиент active 26→14 + бордер 45% ПОД фоном (замер 134 = расчёт), неактивный bg-mantle побитово, точка 4×4, пин fa 10 в слоте 16, disconnect 18×18 глиф 16, метка fs12/500 (line_height 1.169 её не сдвинул), sleeping .55, dndDragging .4. Ниты: точка на 1px ниже; бюджет усечения `CHIP_CHROME_W=62` не учитывает бордер и всегда вычитает место под disconnect.

## Цикл 11: MATCH

чип с пином, меткой и disconnect — как в ц.8.

## Цикл 13: MATCH

Ревью подтвердило метрики чипа целиком. Дополнительно закрыто: цвет сессии в
светлой теме резолвится через `resolveSessionColor` (см. 34) — чип брал
dark-значение.

## Цикл 15: DIVERGES

Ядро подтверждено кадром (h 28.0, зазоры, градиенты, пин, disconnect). Осталось: при `switching` пин пропадает (оригинал анимирует только точку); ширина чипа фиксированная вместо `flex 0 1 180`; `CHIP_CHROME_W` всегда резервирует место под disconnect.

## Цикл 19: DIVERGES

Ядро подтверждено кадром (чип 138.4 × 28, r-md, отступы, градиенты, точка `--text-muted`).
Закрыто: при `switching` пульсирует ТОЛЬКО точка, пин остаётся; бюджет метки у спящего чипа больше на 24 px (кнопки disconnect в DOM нет) + учтены 2 px рамки.
Осталось: механика ширины (расчёт вместо `flex: 0 1 180px`).

## Цикл 23: DIVERGES

Ширина чипа по-прежнему расчётная (`chip_w = (row_w/n − 3).clamp(44,180)`), у оригинала `flex: 0 1 180px` и реальный shrink. Замер сошёлся с формулой: 139.2 при шаге 142.4.

## Цикл 26: DIVERGES

Осталось: порядок каскада `.active` и `.tinted` — в ТЁМНОЙ теме у цветной и
одновременно активной сессии фон берётся из `.tinted` (15→8 %, ховер 22→12 %),
а мы рисуем active (26→14 %), в светлой теме расхождения нет; похоже на баг
оригинала, нужно решение — копировать или нет; ширина чипа считается формулой
вместо `flex: 0 1 180px; min-width: 44px`; transition у `.close` и
`transform: scale` — упоры движка

## Цикл 32: DIVERGES

Закрыто: каскад `.active` / `.tinted` приведён к оригиналу. `.active`
(`SessionTab.module.css:29-34`) объявлен ПЕРЕД `.tinted` (`:37`) при равной
специфичности (0,1,0) — значит у ЦВЕТНОЙ и одновременно активной сессии в
ТЁМНОЙ теме побеждает `.tinted` (15→8 %, ховер 22→12 %), а не `.active`
(26→14 %). В светлой теме `[data-theme=light] .active` (0,2,0) идёт после
light-`.tinted` и выигрывает — там поведение прежнее. Решение принял сам:
задача — парити, значит копируем каскад оригинала, даже если он выглядит как
его недосмотр.

Осталось: ширина чипа считается формулой вместо `flex: 0 1 180px;
min-width: 44px`; transition у `.close` и `transform: scale(1.06)` — упоры
движка
