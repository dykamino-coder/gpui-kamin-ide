# Кадры: N/A

Элемент визуального представления НЕ ИМЕЕТ — сверять нечего, кадры обеих
сторон отсутствуют законно.

Мёртвый `App.module.css` (`.app`/`.workbench`/`.center`) оригинала — в
рантайме не применяется, в порт не переносился.
