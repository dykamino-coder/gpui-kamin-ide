# 159 — кадр невозможен, и это не пропуск

`App.module.css:1-22` — МЁРТВЫЙ файл оригинала: классы `.app`, `.workbench`,
`.center` не импортируются ни одним компонентом (подтверждено `INVENTORY.md`).
Мёртвый CSS не рисует ничего, снимать нечего ни с одной из сторон.

Досье существует ради полноты инвентаря: элемент зафиксирован, разобран и
помечен как не подлежащий переносу в порт.
