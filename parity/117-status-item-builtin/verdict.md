# 117 — verdict (review cycle 1)
VERDICT: MATCH
item-рецепт/тона/тултипы/глифы — 1:1 (высота пилюли — корень в 116).

## Цикл 2: MATCH

## Цикл 5: MATCH

Встроенный элемент статуса: gap4/px8/r-xs/fs11/codicon 12/hover bg-surface 60% + text-primary; тона ok/warn/muted 1:1.

## Цикл 6: MATCH

Встроенный элемент статуса 1:1.

## Цикл 15: MATCH

Встроенный item: gap 4, px 8, r-xs, глиф 12 (`!important`), тона ok/warn, hover `bg-surface 60 %` + text-primary.

## Цикл 17: MATCH

Встроенный item: gap 4, px 8, r-xs, глиф 12 (`!important`), тона ok/warn, hover `bg-surface 60 %` + text-primary.

## Цикл 20: DIVERGES

НОВОЕ: `.item:hover { color: text-primary }` не отрабатывал — замеры показали, что фон меняется, а цвет нет.
Закрыто: подпись и глиф красятся через `group_hover`.
Ждёт пиксельного подтверждения.

## Цикл 23: MATCH

Ховер «4 active»: фон #1d1d28 → #303141 (bg-surface 60 %), и глиф, и подпись #a6e3a1 → #cfd4e2 — зелёный уходит полностью.

## Цикл 24: DIVERGES

(осознанное отступление, см. ниже)

Живой прогон (юзер): по ховеру подпись счётчика ПРЫГАЛА в кегле, а красный
`$(error)` превращался в «тофу»-квадрат. Корень — не наш CSS, а vendored gpui:
`StyleRefinement.text` не помечен `#[refineable]`, поэтому `Style::refine`
заменял текстовый под-рефайнмент ЦЕЛИКОМ. Ховер, задающий один `text_color`,
стирал `font_size` и `font_family` базы — отсюда и скачок кегля, и потеря
шрифта кодиконов. Патч `refine_merge_text` в `compute_style_internal` сливает
текст поле-в-поле (маркер «KaminIDE patch», реестр `scripts/vendor_patches.py`
обновлён: div.rs 5 маркеров). Проверено кадром: и таб стрипа, и `⊗ 3` больше
не меняют размер, глиф остаётся кодиконом.

ОТСТУПЛЕНИЕ от оригинала (по прямой просьбе пользователя): встроенные
счётчики стали кликабельными. У оригинала это `<button tabIndex={-1}>` без
`onClick` (`StatusBar.tsx:147-159`) — чисто информационные. Теперь:
«N active» → Customize/Extensions, «N cmds» → палитра команд.

## Цикл 26: DIVERGES

Закрыто: таб-стоп теперь только у КЛИКАБЕЛЬНОГО счётчика. Оригинал ставит
`tabIndex={-1}` с прямым комментарием «keeps them out of the tab order», а мы
чеканили кольцо всем, включая «N failed», «N off», UTF-8 и EOL, и сами себе
противоречили — `contrib()` некликабельный элемент из таб-порядка исключает.

Осталось: клики по встроенным счётчикам — зафиксированное отступление по
просьбе пользователя, у оригинала их нет; пункт 4 в `ours.md` устарел


## Цикл 35: DIVERGES

`ours.md` приведён к коду: пункт 4 утверждал, что высота элемента идёт от
контента, хотя обёртка давно тянется `h_full` до высоты бара
(`status_bar.rs:403`). Отступление по кликам вынесено в отдельный пункт,
чтобы не терялось при чтении.

Осталось: клики по встроенным счётчикам — зафиксированное отступление по
просьбе пользователя, у оригинала их нет
