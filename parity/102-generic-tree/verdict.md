

## Цикл 4: DIVERGES

Generic TreeView не реализован (только файловое дерево).

## Цикл 8: DIVERGES

Generic TreeView не реализован.

## Цикл 10: DIVERGES

Generic-дерево не реализовано. ★ НОВОЕ: это тот же компонент, что элемент 139 (sample-tree) — в продуктовом UI не используется вовсе, только в семплах Design; закрытие 139 закрывает и 102/103.

## Цикл 13: DIVERGES

РЕАЛИЗОВАН (`ui/tree.rs`). Компонента не было вовсе; ревью показало, что
прежний семпл повторял рецепт file-tree и расходился по каждому свойству
(индент, паддинги, gap, базовый цвет, бокс шеврона, цвет иконки, колонка meta).

Портировано: индент 14, `padding: 4px 8` с инлайновым `paddingLeft`, gap 8,
резервная прозрачная рамка, radius-xs, fs-sm, text-primary, ховер bg-surface
55 %, выделение — градиент accent 26 %→14 % + рамка 45 %, бокс шеврона 14 с
глифом 16, иконка 12 (папка accent-yellow / файл text-muted), колонка `meta`
моно fs-xs, `.treeFrame` семпла (max-w 380, p 8, рамка bg-surface 60 %,
r-sm, bg-base).

Снято на живом окне (`ours.png`): данные `SAMPLE_TREE` оригинала, раскрыты
`src` и `src/host`, выделен `index.ts`.

Осталось: aria-роли; `transition` фона; клик на mouse_down.

## Цикл 13 (проверка ревью): DIVERGES

Ревью зоны 130-159 выписало полную таблицу расхождений старого семпла (12
свойств) — она закрыта портом настоящего `Tree` в этом же цикле: индент 14,
padding 4/8, gap 8, text-primary, бокс шеврона 14 с глифом 16, иконка 12
(папка accent-yellow), колонка `meta`, обёртка `.treeFrame`, данные
`SAMPLE_TREE`, состояние expanded `{src, src/host}` + selected `index.ts`.

Осталось: aria-роли; `transition`; в семпле состояние статично.

## Цикл 13 (ревью зоны): MATCH

`.tree/.subtree` — `list-style:none; margin:0; padding:0`, собственного бокса
не создают; наш `div().flex().flex_col()` эквивалентен. Расхождений нет
(aria-роли вне визуального паритета).

## Цикл 15: MATCH

Generic-дерево: плоская колонка без боксов, данные семпла и рамка `.treeFrame` совпадают.

## Цикл 18: MATCH

Generic-дерево: списки без собственного бокса — плоская колонка эквивалентна.

## Цикл 21: MATCH

Generic-дерево: списки собственного бокса не создают.
