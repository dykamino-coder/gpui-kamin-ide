

## Цикл 4: DIVERGES

Состояния детей — **исправлено волной 7**: кап 100 + шаг 200
(`DIR_RENDER_CAP`/`DIR_RENDER_STEP`, было 200 и «раскрыть всё»), лейбл
«Show N more (M hidden)» + глиф ellipsis, `.emptyChild` «(empty)» и
per-dir «Loading…». Проверить живьём на большой папке (>100 файлов).

## Цикл 8: DIVERGES

Кап 100/шаг 200, «(N hidden)», «(empty)», per-dir «Loading…» — закрыто и проверено живьём. **Индент «Show N more» исправлен волной 15** (был на 12px глубже: лишний `+1` к глубине). Нит: у `.showMore` не должно быть скругления фона ховера.

## Цикл 10: DIVERGES

Кап 100/шаг 200, «Show N more (N hidden)», «(empty)», per-dir Loading — на месте; индент подтверждён закрытым численно. Остался нит: у showMore лишнее скругление RADIUS_XS, в оригинале скругления нет.

## Цикл 12: MATCH

Ревью подтвердило: кап 100 / шаг 200, «Show N more (N hidden)», «(empty)», per-dir
«Loading…», индент без лишнего `+1`, у `showMore` скруглений нет — как в
`.showMore`. Открытых претензий нет.

Отдельно снят мёртвый код: корневая ветка «Loading…» была недостижима (`rows()`
сама пушит строку при промахе кэша) — удалена.

## Цикл 13: DIVERGES

Закрыто: глиф `codicon-ellipsis` у «Show N more» — 16 (база `.codicon`
`skeleton.css:2-5`), был 13.

Осталось: `childCap` живёт по пути в модели и переживает сворачивание, а в
оригинале это `useState` внутри узла и при collapse откатывается к 100.

## Цикл 13 (ревью зоны): MATCH

Ревью подтвердило: кап 100 / шаг 200, «Show N more (M hidden)», «(empty)»,
per-dir «Loading…», глиф ellipsis 16, py 3, без скруглений, ховер со сменой
цвета.

Уточнение к вердикту цикла 12: `child_cap` живёт в модели по пути и не
сбрасывается; в оригинале `useState` сбрасывается при РАЗМОНТИРОВАНИИ узла,
то есть при сворачивании предка, а не самой папки.

## Цикл 14: MATCH

Подтверждено повторно: кап 100 / шаг 200, «Show N more (M hidden)», глиф
ellipsis 16, `py 3` без скруглений, ховер со сменой цвета, «(empty)» и
«Loading…» fs-xs/muted.

## Цикл 15: MATCH

Состояния детей: `.loading`/`.emptyChild`/`.showMore` с индентом `depth+1`, кап 100 + шаг 200, текст «Show N more (M hidden)».

## Цикл 18: MATCH

Состояния детей: кап 100 + шаг 200, «Show N more (M hidden)», глиф 16, py 3, ховер со сменой цвета, «(empty)»/«Loading…» fs-xs muted.

## Цикл 21: DIVERGES

Закрыто: ховер «Show N more» красит и глиф (`group_hover`).

## Цикл 23: MATCH

«Show N more (N hidden)», отступ без +1, `py 3`, ховер фон+глиф — расхождений не найдено.
