# 112 — verdict (review cycle 1)
VERDICT: DIVERGES
Нет shadow 0/6/24/30%; w240/max-h400 vs min200-max360/60vh; item 12/4 gap8 primary
vs 8/5 gap6 secondary; hover tint vs bg-surface-hover; нет overflowItemActive и pin;
кнопка «N v» vs 24×24 chevron. dirty-orange/Esc/клик-вне/бордер — подтверждены.

## Цикл 2: DIVERGES
max-h 400 vs 60vh; нет overflowItemActive и pin в item; триггер «N ▾» vs 24×24 chevron+тултип; border text-primary6% vs divider-soft (то же значение — ок фактически).

## Цикл 5: DIVERGES

Overflow-меню табов: min-w200/max-w360, shadow 0/6/24/.3, item 5/8 gap6, dirty-точка — исправлено. Остаток: триггер должен быть «N ▾» (h24 px6 + счётчик), у нас 24×24 chevron; нет `.overflowItemActive` (accent 16%) и pin-иконки в пункте; `max_h 400` вместо 60vh; `top 28` вместо `calc(100% + 2px)`.

## Цикл 6: DIVERGES

`max_h 400` вместо 60vh, `top 28` вместо `calc(100%+2px)`, нет active-подсветки и pin-иконки. Уточнение ц.6: триггер у оригинала — 24×24 chevron с тултипом «More open files», у нас «N ▾» без тултипа (цикл 5 сформулировал направление наоборот).

## Цикл 7: DIVERGES

max-height 60vh против 400; top 26 против 28; нет overflowItemActive и pinIcon; триггер 24x24 chevron + тултип против «N» без тултипа.

## Цикл 11: DIVERGES

Закрыто: `max-height` считается от вьюпорта (60vh) вместо фиксированных 400 и у меню появилась прокрутка — раньше длинный список просто обрезался.

Осталось: сверить кадром; `ours.md` переписан по факту кода (min-w 200 / max-w 360, item 8/5, dirty accent-orange, Escape и клик-мимо закрывают).

## Цикл 12: DIVERGES

ИСПРАВЛЕНА МОЯ ОШИБКА цикла 11: прокрутки у меню НЕ БЫЛО — `.overflow_hidden()`
в конце цепочки затирал `.overflow_y_scroll()` (оба пишут в `base_style.overflow`).
Лишний `overflow_hidden` убран, `max-height` = 60vh от реального окна, `top` 26
(`calc(100% + 2px)`), тень взята из тем-зависимого словаря `shadows::dropdown()`.

Осталось: меню перечисляет только СКРЫТЫЕ табы, оригинал — все открытые файлы;
нет `.overflowItemActive` и pin-иконки; ховер триггера `text-primary 8%` вместо
`bg-surface-hover`; у триггера нет тултипа «More open files».

## Цикл 12 (добивка): DIVERGES

Закрыто дополнительно: ховер триггера — `--bg-surface-hover` (был `text-primary 8%`,
две копии логики разошлись); у триггера появился тултип «More open files».

Осталось: меню перечисляет только скрытые табы (оригинал — все открытые файлы);
нет `.overflowItemActive` и pin-иконки.

## Цикл 12 (добивка 2): DIVERGES

Закрыто: меню перечисляет ВСЕ открытые файлы (`FileViewerTabs.tsx:214`), а не только
не влезшие в стрип; активный пункт получил `.overflowItemActive` — accent 16% +
text-primary.

Осталось: pin-иконка в пункте; кадр состояния.

## Цикл 13: DIVERGES

Закрыто по находкам ревью: тень — своя `0 6px 24px rgb(0 0 0 / 30%)`, а не
токен `--shadow-dropdown`; триггер стал квадратом 24×24 с одним chevron
(счётчик «N» был нашей добавкой); пункты вплотную (лишний gap 1 убран); ховер
красит и текст; у активного пункта ховер НЕ перебивает accent-тинт; в пункте
появились pin-иконка codicon-pinned 11 @ .7 и глиф ● 10 вместо нарисованного
круга; у пункта есть тултип с полным путём.

Осталось: порядок пунктов — исходный, а не `sortTabs` (pinned-first).

## Цикл 14: DIVERGES

Закрыто: кегль пина в пункте меню 11 → **16** (тот же каскад).

Ревью подтвердило правки ц.13 целиком: триггер 24×24 + chevron 16 + mr 4,
тень `0 6 24 / 30%`, пункты без gap, ховер красит текст, активный пункт не
перебивается, тултип с полным путём.

Осталось: меню сдвинуто на 4px левее — `right: 0` в оригинале считается от
паддинг-бокса `.overflow` (у него `padding-right: space-1`), а у нас от самой
кнопки.

## Цикл 15: DIVERGES

Осталось: `.overflow` оборачивает и кнопку, и меню (у нас `mr` на кнопке) — меню на 4 px правее.

## Цикл 17: MATCH

Меню переполнения табов: min-w 200 / max-w 360 / max-h 60vh, `top: 26` = `calc(100% + 2px)`, своя тень `0 6 24 rgba(0,0,0,.3)`, item 5/8 + `.overflowItemActive` accent 16 %; перечисляет все табы.

## Цикл 20: DIVERGES

Вердикт ц.17 «MATCH» снят: `.overflow { padding-right: 4 }` оборачивает и кнопку, и меню, поэтому правый край меню = правый край РЯДА; у нас `mr` на кнопке → меню на 4 px левее. Плюс ховер-цвет пункта.

## Цикл 23: MATCH

Меню переполнения: правый край 1028.8 против 1028.0 у ряда табов; пункт #adb3c7 → #cfd4e2 на bg-surface-hover, активный на accent 16 %.
