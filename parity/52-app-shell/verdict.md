# 52 — verdict (review cycle 1)
VERDICT: DIVERGES
MATCH: фон+эллипсы, левый гуттер 4, mainColumn min-w 100, welcome в main-карте.
Расхождения: нет pr(4) у body (rail вплотную к краю окна); гэп activity-bar/
sidebar 4 vs 8; noSessions не скрывает File/Right/MainBottom; fileFills/
rightFills (flex 1 1 0) не реализованы.

## Цикл 5: DIVERGES

`.body gap 8` между activity-bar и сайдбаром даёт только 4 (бар — прямой ребёнок body без обёртки, зазор несёт лишь `pl 4` сайдбара) → нужен `pr(4)` у бара. `noSessions` не гасит File/Right/MainBottom (оригинал `AppLayout.tsx:55-79` опускает их при нуле сессий). fill-режимы (`fileFills`/`rightFills` = flex 1 1 0) не реализованы. `pl/pr 4` и welcome-в-карте — подтверждены.

## Цикл 6: DIVERGES

Зазор 8 закрыт (`pr 4` у бара), но border-box съел ширину — плитки уехали на 2px влево (замер ц.6). **Исправлено волной 10**: `w(ACTIVITY_BAR_WIDTH + 4).pr(4)`. Осталось: `noSessions` не гасит File/Right/MainBottom, fill-режимы.


## Цикл 12: DIVERGES

Закрыто: `noSessions` теперь гасит File-панель, Right-панель и Main-Bottom-ящик —
пустой старт показывает только сайдбар и welcome, как `AppLayout.tsx:51,69,72,73`.
Тумблеры видимости не трогаются: раскладка возвращается сама при открытии сессии.
Customize-режим исключён из правила (свой focused-режим).

Осталось: fill-режимы (`fileFills`/`rightFills`) — колонка не растягивается на
освободившееся место, когда main скрыт.

## Цикл 12 (добивка): DIVERGES

Закрыто: fill-режимы. `fileFills`/`rightFills` считаются как в `AppLayout.tsx:53-54`;
колонки получили `min-width: 100` и перестали быть `flex-shrink: 0`. Замер живьём:
скрытие main растягивает File-панель 440 → 797.6 px, возврат — обратно 440.

Осталось: сверить кадром пустого старта (нужно закрыть все сессии пользователя).

## Цикл 13: DIVERGES

Закрыто: `.body { overflow: hidden }` — горизонтальное переполнение колонок
теперь режет каркас (раньше не резалось ничем).

Осталось: `color: var(--text-primary)` на корне задаётся точечно, а не
наследованием (осознанно).

## Цикл 13: DIVERGES

Закрыто: сохранённая ширина снова означает ширину КАРТЫ, а не обёртки.
`gap_wrap` съедает по 4px, поэтому file-колонка была на 8px, а правая — на 4px
уже сохранённого числа (у сайдбара компенсация уже стояла). Теперь обёртки
берут `file_w + 8` и `right_w + 4`.

Осталось: `color: var(--text-primary)` на корне у нас не наследуется — цвет
ставится точечно (осознанно).

## Цикл 16: MATCH

App-shell: гуттеры 4, межпанельные зазоры ровно 8.0, фон `bg_sidebar` + два запечённых эллипса — эквивалент `transparent 60 %`.

## Цикл 19: MATCH

App-shell: зазоры карт 8.0 с обеих сторон, гуттер 4 — замер строки y=600.
