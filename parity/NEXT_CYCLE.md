# Задание на следующий цикл сверки

Сгенерировано `parity/cycle.py` из остатка гейта. После правок: `python parity/gate.py --list`.

## Зона 52-91 — Панели и экраны: цикл 23

Порт: `crates/shell/src/{ui/customize.rs, ui/design_panel.rs, ui/logs_panel.rs, ui/extensions_panel.rs, ui/problems.rs, ui/panel_placeholder.rs, ui/term_toolbar.rs}`. Оригинал: `%PROJECTS%\kamin-ide\src\renderer\components/{main,settings,right-panel,main-bottom-panel,problems,terminal}/*` + `theme/*.css`.

По КАЖДОМУ пункту ниже ответь «закрыто» или «осталось» с числом/строкой кода; новое — отдельным списком. Мерить пиксели скриптом (PIL/numpy) в СВОЮ временную папку, в `parity/` не писать. DPR обеих сторон 1.25; в наших кадрах бывает рамка захвата ~9 физ. px слева и сверху — калибруйся по ней перед выводами.

Ограничения gpui (НЕ дефект, но упоминать, если влияет): нет `letter-spacing`, нет `transform`, `svg().text_color()` не реагирует на hover, ровно ОДИН `.hover()` на элемент, у `Stateful` нет `self_center()`, `Input` берёт кегль и паддинги из своего `Size`, а не из обёртки.

Про эллипсис: `text_ellipsis()` ВЫСТАВЛЯЕТ `TextOverflow::Truncate("…")` (`vendor/gpui/src/styled.rs:83-88`), но на экране для `.child(SharedString)` многоточие не появлялось — текст резался родительским `overflow_hidden` (скрин-сверка, см. шапку `ui/text_fit.rs`). Поэтому «нет …» — валидная претензия ТОЛЬКО если подтверждена кадром; править её нужно через `ui::text_fit::fit`, а не добавлением `text_ellipsis()`.

### Претензии к закрытию (20)

- **56 right-panel-column** (ц.19)
  НОВОЕ: при ресайзе окна оригинал масштабирует `rightPanelWidth` по фактору (`layout-autosave.ts:189-196`); у нас ширина правой колонки меняется только драгом. ## Цикл 19 (доработка): DIVERGES Закрыто: правая колонка масштабируется вместе с окном (`max(100, round(w · factor))` при смене ширины вьюпорта), а не ждёт следующего драга. Ждёт подтверждения замером после ресайза.
- **63 file-panel-top-card** (ц.19)
  Инверсия ведущего размера: у оригинала `.topCard { flex: 1 }` (остаток), у нас доля `1 − bottom_ratio`, а низ — flex-остаток. ## Цикл 19 (доработка): DIVERGES Закрыто: верх — `flex: 1` (остаток), как `.topCard`; ведущей стала нижняя карта. Ждёт подтверждения замером после ресайза окна.
- **64 file-panel-split-handle** (ц.19)
  Минимум нижней карты: оригинал жёстко 100 px, у нас `BOTTOM_RATIO_MIN 0.1` от body_h = 83.4 px; база доли — body_h вместо `innerHeight`. ## Цикл 19 (доработка): DIVERGES Закрыто: драг ведёт ПИКСЕЛЯМИ с жёстким минимумом 100 (`max(100, startHeight − deltaY)`), ratio считается от высоты ОКНА и используется только для персиста. Ждёт подтверждения замером.
- **65 file-panel-bottom-card** (ц.19)
  Замер: `central-bottom` h = 317.6 против 355 у оригинала при том же `ratio 0.39499` — высота считается от body_h, а не от высоты окна; плюс у оригинала `flex-shrink: 0`, у нас низ — остаток. ## Цикл 19 (доработка): DIVERGES Закрыто: высота нижней карты = `max(100, round(ratio · innerHeight))` + `flex-shrink: 0`. Замер после правки: `central-bottom` h = 352.8 против 317.6 до неё (расчёт оригинала ≈
- **67 browser-pane** (ц.19)
  Закрыто: `.viewport` браузера снова 12 (радиус хранится по зоне). Осталось: `onFocus → select()` в адресной строке.
- **70 webview-loading-skeleton** (ц.19)  ⚠ нет пары кадров — вердикт не подтверждён
  Закрыто: встроенные вью больше не идут мимо скелета — `webview_panel`/`webview_body` оказались мёртвыми и удалены, всё идёт через `webview_body_dyn`. Осталось: contributed-страница Customize рисует голое «Loading…» вместо скелета. ## Цикл 19 (доработка): DIVERGES Закрыто: contributed-страница Customize показывает ТОТ ЖЕ скелет загрузки, что и панели (было голое «Loading…»). Мёртвые `webview_panel`
- **72 chat-switch-skeleton** (ц.19)
  Накрывашка переключения чата (brand 96 / glow 150 / logo 64 / caption / bar 180×3) не реализована. ## Цикл 22 (правка): DIVERGES Закрыто: накрывашка переключения сессии портирована (`ui/chat_switch_skeleton.rs`) — brand 96, glow 150 (запечённый спрайт, спад 66 %, «дыхание» через размер + opacity), логотип 64 с «парением» через `top`, подпись «Loading conversation…» fs-sm text-muted, полоса 180×3 r
- **75 webview-view-anchor** (ц.19)
  Радиус рамки вью 16 закрыт. Осталось: `.frameFlush` для contributed-страницы Customize (у нас инсеты вместо edge-to-edge).
- **76 persistent-webview-layer** (ц.19)
  Осталось: накрывашка чата (см. 72). Снято: расхождение по GDI-региону — его наполняет только модалка, и её радиус совпадает. ## Цикл 22 (правка): DIVERGES Закрыто: накрывашка переключения сессии портирована (`ui/chat_switch_skeleton.rs`) — brand 96, glow 150 (запечённый спрайт, спад 66 %, «дыхание» через размер + opacity), логотип 64 с «парением» через `top`, подпись «Loading conversation…» fs-sm 
- **77 welcome-placeholder** (ц.19)
  Закрыто: спад свечения 68 % (`bake_glow_edge(220, accent, 0.26, 0.68)`), метрики 112/220/38.4/700 совпали. Осталось: `drop-shadow` у лого и `letter-spacing -0.02em` — упоры gpui.
- **78 customize-content-panel** (ц.19)
  Осталось: хедер (`h1` = имя вью + «Contributed by an extension.») для contributed-страницы Customize; `.bodyFlush`. Встроенные страницы 1:1 (замер header 20/24/12, body 16/24).
- **79 design-panel-shell** (ц.19)
  Осталось: скроллер — у оригинала скроллит `.body` панели Customize, у нас `design-panel` скроллит сам внутри уже скроллящегося тела (полоса на 24 px левее); ритм секций через `mb` вместо `gap` корня.
- **80 logs-panel** (ц.19)
  Закрыто: дебаунс фильтра 150 мс, сброс фильтра при смене канала, ховер имени канала (`group_hover`), пустой буфер больше не подписывается (у оригинала `<pre>` просто пуст). Осталось: фокус-рамка поиска, полный буфер вместо хвоста 400 строк, stick-to-bottom.
- **81 system-log-panel** (ц.19)
  Закрыто: тексты пустого состояния дословно; условие «нет логов» считается по `entries.is_empty()`, а не по фильтру. Осталось: фокус-рамка поиска, grid-колонка `source`, тултип абсолютного времени.
- **82 settings-panel** (ц.19)
  Осталось: нативный `input[type=checkbox]` и `disabled` только на инпуте (у нас гаснет вся строка).
- **83 legacy-bridge-card** (ц.19)
  Осталось: состояние `busy` карточки (подпись «Removing…», кнопка неактивна) и разбивка текста модалки на два абзаца. ## Цикл 19 (доработка): DIVERGES Закрыто: состояние `busy` — подпись «Removing…» и гашение кнопки, пока идёт удаление. Осталось: разбивка текста модалки на два абзаца.
- **85 extension-row** (ц.19)
  Осталось: реальные иконки расширений (data-URL + кэш) — всегда фоллбэк-кодикон 16 в боксе 26×26.
- **89 terminal-toolbar** (ц.19)
  Закрыто: `.tab { flex: 0 1 auto }` — табы сжимаются (min 80 / max 220). Осталось: переполнение через реальный `overflow-x` со smooth-скроллом на 80 % ширины вместо окна по индексу.
- **90 terminal-shell-menu** (ц.19)  ⚠ нет пары кадров — вердикт не подтверждён
  Закрыто: звезда снаружи `.menuItem`, ховер только на строке-кнопке, глиф по профилю, список всех шеллов. Осталось: центровка поповера по якорю вместо `right: 0`. ## Цикл 19 (доработка): DIVERGES Закрыто: меню шеллов центрируется ПО ЯКОРЮ (`left = a.left + a.width/2 − p.width/2` с клампом гуттером 8, пересчитанным в координаты якоря), а не прижато `right: 0` — при min-w 200 и кнопке 28 это было ~86
- **91 terminal-session-host** (ц.19)
  НОВОЕ: `cursorBlink: true` у оригинала — у нас курсор статичный. ## Цикл 19 (доработка): DIVERGES Закрыто: курсор терминала мигает периодом 1.2 с (`cursorBlink: true`), был статичным. Ждёт подтверждения кадром.

Формат ответа: `NN <имя>: MATCH|DIVERGES` + при DIVERGES 1-3 строки (оригинал файл:строка/значение → у нас файл:строка/значение → как чинить). В конце — сводка и список закрытых пунктов.

## Зона 108-129 — Редактор, оверлеи, статус: цикл 23

Порт: `crates/shell/src/{ui/quick_open.rs, ui/command_palette.rs, ui/find_in_files.rs, ui/workspace_symbols.rs, ui/modal.rs, ui/quick_pick.rs, ui/toasts.rs, ui/tooltip.rs, ui/status_bar.rs, overlay.rs}`. Оригинал: `%PROJECTS%\kamin-ide\src\renderer\components/{overlays,status-bar,webview}/*` + `theme/*.css`.

По КАЖДОМУ пункту ниже ответь «закрыто» или «осталось» с числом/строкой кода; новое — отдельным списком. Мерить пиксели скриптом (PIL/numpy) в СВОЮ временную папку, в `parity/` не писать. DPR обеих сторон 1.25; в наших кадрах бывает рамка захвата ~9 физ. px слева и сверху — калибруйся по ней перед выводами.

Ограничения gpui (НЕ дефект, но упоминать, если влияет): нет `letter-spacing`, нет `transform`, `svg().text_color()` не реагирует на hover, ровно ОДИН `.hover()` на элемент, у `Stateful` нет `self_center()`, `Input` берёт кегль и паддинги из своего `Size`, а не из обёртки.

Про эллипсис: `text_ellipsis()` ВЫСТАВЛЯЕТ `TextOverflow::Truncate("…")` (`vendor/gpui/src/styled.rs:83-88`), но на экране для `.child(SharedString)` многоточие не появлялось — текст резался родительским `overflow_hidden` (скрин-сверка, см. шапку `ui/text_fit.rs`). Поэтому «нет …» — валидная претензия ТОЛЬКО если подтверждена кадром; править её нужно через `ui::text_fit::fit`, а не добавлением `text_ellipsis()`.

### Претензии к закрытию (17)

- **108 file-viewer-wrapper** (ц.20)
  Осталось: обёртка `.viewer` (margin 0/6/6, bg-mantle, r-md) — у нас инсеты 4.8/4.0; паддинги `.body` 8/0/10; breadcrumb-строка и кнопка «Save Ctrl+S» — сверх оригинала.
- **110 file-viewer-tabs-strip** (ц.20)
  Осталось: переполнение — оригинал рендерит ВСЕ табы и режет крайний `overflow: hidden`, у нас лишние выкидываются (замер: 161 px пустоты и сломанный порядок); `.dropIndicator` по центру зазора и вставка ПОСЛЕ последнего.
- **111 file-viewer-tab** (ц.20)
  Закрыто: `.tabDragging { opacity: .3 }` подтверждён кадром. Осталось: ховер должен красить и подпись (лечится `group_hover`), `line-height: 1` у dirty-точки, иконка вебвью-таба, `letter-spacing` и `transition` — упоры.
- **112 file-viewer-tabs-overflow** (ц.20)
  Вердикт ц.17 «MATCH» снят: `.overflow { padding-right: 4 }` оборачивает и кнопку, и меню, поэтому правый край меню = правый край РЯДА; у нас `mr` на кнопке → меню на 4 px левее. Плюс ховер-цвет пункта.
- **113 monaco-editor-host** (ц.20)
  Осталось: карточка `.error` («Failed to open: …», моно, accent-red) не портирована.
- **114 webview-panel-view** (ц.22)
  Закрыто: - **loader-cover** поверх вебвью: `absolute inset 0`, `bg-surface`, спиннер 22×22 (кольцо `border 2.5` text-primary 16 %, верхняя четверть accent-action, вращение `.7s linear`), фейд 180 мс. Держится до пинга страницы (`__kaminReady` оригинала), но не дольше `READY_FALLBACK_MS` 1200 мс. Замер: бокс спиннера 28 device px при scale 1.25 = 22.4 лог., фон крышки `#3d3f51` = `--bg-surface`. - 
- **115 webview-tab-icon** (ц.22)
  Без изменений: `WebviewTabIcon` требует вебвью-панелей как вкладок редактора (`createWebviewPanel`), которых в порте пока нет. Зависит от того же куска, что и остаток 114.
- **117 status-item-builtin** (ц.20)
  НОВОЕ: `.item:hover { color: text-primary }` не отрабатывал — замеры показали, что фон меняется, а цвет нет. Закрыто: подпись и глиф красятся через `group_hover`. Ждёт пиксельного подтверждения.
- **118 status-item-contributed** (ц.20)
  Ревьювер снял ложную претензию: элемент С `item.color` ведёт себя верно (inline-стиль перебивает ховер), а `parse_css_color` подтверждён. Закрыто: ховер-цвет для элемента БЕЗ цвета — через `group_hover`.
- **120 status-version-update** (ц.20)
  Вердикт ц.14 по ховеру бренда снят и закрыт заново через `group_hover`. Осталось: состояние `downloading` с прогресс-заливкой и установка апдейта в приложении. ## Цикл 22 (правка): DIVERGES Закрыто: клик по пилюле ставит апдейт ВНУТРИ приложения (`updater::install`: скачивание через `ureq` с отчётом прогресса → запуск инсталлятора `/S` → выход), в пилюле состояние `downloading` — заливка `accent-p
- **121 confirm-modal** (ц.20)
  Геометрия 1:1 (замер: коробка 404.8 × 124.8, центр точный). Осталось: жирное имя в теле (санитизированный HTML), «?» в заголовках, автофокус Confirm + Enter, `fadeIn` — упор.
- **122 prompt-modal** (ц.20)
  Закрыто: ряд ввода получил `--fs-md` 13 и нулевой собственный бокс `Input`. Осталось: рамка accent только на `:focus`, `focus() + select()` при открытии. ## Цикл 22 (правка): DIVERGES Закрыто: рамка инпута prompt-модалки красится accent ТОЛЬКО в фокусе (`.input:focus`), иначе `--bg-surface`; состояние читается у самого `InputState` через `focus_handle(cx).is_focused(window)`. Раньше accent стоял в
- **123 quick-pick-modal** (ц.20)  ⚠ нет пары кадров — вердикт не подтверждён
  Закрыто: `.list { flex: 1 }` — ряд Cancel/OK прижат к низу панели (под ним зияло ~290 px); кегль ряда ввода 13 и его бокс. Осталось: `gap` между чекбоксом и лейблом в строке с `detail` (его съедает `ml_auto`).
- **124 quick-open** (ц.20)
  Закрыто: кегль ряда ввода 13. Осталось: `backdrop-filter` (упор) и лишний пол 320 у `max-width`.
- **125 find-in-files** (ц.20)
  Закрыто: кегль ряда ввода 13. Осталось: однострочный сниппет с общим эллипсисом (у нас три бокса — при длинном префиксе совпадение выталкивается), `backdrop-filter`, пол 320.
- **126 workspace-symbols** (ц.20)
  Ревьювер снял давнюю ложную претензию: `symbol-method/function/constructor` в самом шрифте — ОДИН глиф `ea8c`, у нас верно. Закрыто: кегль ряда ввода 13. Осталось: цвет глифа на активной строке в светлой теме, кап 100 строк.
- **127 command-palette** (ц.20)
  Закрыто: кегль ряда ввода 13. Осталось: `.inputRow` через паддинги 12/16 вместо фикс-высоты 44 (разница 0.3 px).

Формат ответа: `NN <имя>: MATCH|DIVERGES` + при DIVERGES 1-3 строки (оригинал файл:строка/значение → у нас файл:строка/значение → как чинить). В конце — сводка и список закрытых пунктов.

## Зона 92-107 — Дерево файлов: цикл 23

Порт: `crates/shell/src/{ui/file_list.rs, ui/file_menu.rs, icon_theme.rs}`. Оригинал: `%PROJECTS%\kamin-ide\src\renderer\components/file-tree/*` + `theme/*.css`.

По КАЖДОМУ пункту ниже ответь «закрыто» или «осталось» с числом/строкой кода; новое — отдельным списком. Мерить пиксели скриптом (PIL/numpy) в СВОЮ временную папку, в `parity/` не писать. DPR обеих сторон 1.25; в наших кадрах бывает рамка захвата ~9 физ. px слева и сверху — калибруйся по ней перед выводами.

Ограничения gpui (НЕ дефект, но упоминать, если влияет): нет `letter-spacing`, нет `transform`, `svg().text_color()` не реагирует на hover, ровно ОДИН `.hover()` на элемент, у `Stateful` нет `self_center()`, `Input` берёт кегль и паддинги из своего `Size`, а не из обёртки.

Про эллипсис: `text_ellipsis()` ВЫСТАВЛЯЕТ `TextOverflow::Truncate("…")` (`vendor/gpui/src/styled.rs:83-88`), но на экране для `.child(SharedString)` многоточие не появлялось — текст резался родительским `overflow_hidden` (скрин-сверка, см. шапку `ui/text_fit.rs`). Поэтому «нет …» — валидная претензия ТОЛЬКО если подтверждена кадром; править её нужно через `ui::text_fit::fit`, а не добавлением `text_ellipsis()`.

### Претензии к закрытию (10)

- **92 file-tree-root** (ц.21)
  Закрыто: ховер строки красит лейбл (`group_hover`). Осталось: декорация корневой строки (цвет/тултип/бейдж), спиннер корня, скроллбар оверлеем с инсетом 3.2 вместо 8 px в потоке, `.flash`.
- **94 file-tree-folder-row** (ц.21)
  Закрыто: ховер строки красит лейбл (замер ц.21: ink не менялся — `.hover().text_color()` до вложенного не доходит). Осталось: `.dropTarget`, драг папок, Shift-диапазон, клавиатура строки, вращение спиннера, `.flash`, «…». ## Цикл 22 (правка): DIVERGES Закрыто: «…» при усечении. Многоточие в порте дописывает `text_fit`, движок его не рисует; добавлен `fit_approx` (оценка по числу символов, ±1 симво
- **95 file-tree-file-row** (ц.21)
  Закрыто: ховер лейбла. Осталось: OS-drag с мультивыделением, Shift-диапазон, клавиатура, «…». ## Цикл 22 (правка): DIVERGES Закрыто: «…» при усечении. Многоточие в порте дописывает `text_fit`, движок его не рисует; добавлен `fit_approx` (оценка по числу символов, ±1 символ) и бюджеты по ширине контейнера из probe прошлого кадра. Проверено кадром: «Create new docx d…». ## Цикл 22 (правка): DIVERGES
- **96 file-tree-children-states** (ц.21)
  Закрыто: ховер «Show N more» красит и глиф (`group_hover`).
- **98 file-tree-header-toolbar** (ц.22)
  Закрыто: спиннер Indexing вращается. Глиф `codicon-loading` вынут из `codicon.ttf` в SVG-ассет (`icons/codicon-loading.svg`) и крутится `with_transformation(rotate)` + `with_animation`, кадры дискретные — `steps(30)` за 1.5 с, как `codicon-modifier-spin` (`codicon.css:35-40`). Осталось: `letter-spacing .08em` у заголовка (в gpui межбуквенного интервала нет — движковый предел).
- **99 tree-icon-img** (ц.21)
  Осталось: светлотемный `filter`, карты `rootFolder*`, `languageIds`, слой `doc.light`. ## Цикл 22 (правка 3): MATCH ★ Закрыто всё оставшееся по иконкам дерева. 1. **Светлый фильтр** `saturate(3.2) brightness(0.7)` (`TreeIcon.module.css:6`). CSS-фильтров в gpui нет — ту же матрицу применяем к hex-цветам ВНУТРИ SVG (`icon_light.rs`), результат кэшируется по ключу источника. Замер: строка `.android`,
- **100 file-context-menu** (ц.21)
  Закрыто: `probe_area` меряет PADDING-box — паддинги больше не учитываются дважды (было −5.2 px по X и переворот на 9 px раньше); вертикальный кламп снят, у side bottom top остаётся на курсоре. Осталось: `visibility:hidden` до первого замера, закрытие по скроллу, тексты удаления и ветка зарезервированных имён.
- **101 file-context-submenu** (ц.21)
  Закрыто: Y каскада по ЦЕНТРУ строки «Open In» (было +18 px), X от неразутой ширины, `overflow-y: auto` и `max-width`. Осталось: переворот влево только при наличии места.
- **105 contributed-tree-node-row** (ц.21)
  Закрыто: ховер строки красит лейбл. Осталось: `treeReveal`, DnD, «…». ## Цикл 22 (правка): DIVERGES Закрыто: «…» при усечении. Многоточие в порте дописывает `text_fit`, движок его не рисует; добавлен `fit_approx` (оценка по числу символов, ±1 символ) и бюджеты по ширине контейнера из probe прошлого кадра. Проверено кадром: «Create new docx d…». ## Цикл 22 (правка 2): MATCH Закрыт `treeReveal`: `ka
- **106 contributed-tree-checkbox** (ц.21)
  Закрыто: рамка чекбокса следует ховеру строки. Осталось: клавиатурный тоггл Space/Enter.

Формат ответа: `NN <имя>: MATCH|DIVERGES` + при DIVERGES 1-3 строки (оригинал файл:строка/значение → у нас файл:строка/значение → как чинить). В конце — сводка и список закрытых пунктов.

## Зона 130-159 — Токены, семплы, глобальные стили: цикл 23

Порт: `crates/shell/src/{ui/design_panel.rs, ui/shadows.rs, ui/glint.rs, crates/theme/src/lib.rs, crates/metrics/src/lib.rs}`. Оригинал: `%PROJECTS%\kamin-ide\src\renderer\components/main/{DesignPanel,design-sections,component-samples*}, theme/*.css` + `theme/*.css`.

По КАЖДОМУ пункту ниже ответь «закрыто» или «осталось» с числом/строкой кода; новое — отдельным списком. Мерить пиксели скриптом (PIL/numpy) в СВОЮ временную папку, в `parity/` не писать. DPR обеих сторон 1.25; в наших кадрах бывает рамка захвата ~9 физ. px слева и сверху — калибруйся по ней перед выводами.

Ограничения gpui (НЕ дефект, но упоминать, если влияет): нет `letter-spacing`, нет `transform`, `svg().text_color()` не реагирует на hover, ровно ОДИН `.hover()` на элемент, у `Stateful` нет `self_center()`, `Input` берёт кегль и паддинги из своего `Size`, а не из обёртки.

Про эллипсис: `text_ellipsis()` ВЫСТАВЛЯЕТ `TextOverflow::Truncate("…")` (`vendor/gpui/src/styled.rs:83-88`), но на экране для `.child(SharedString)` многоточие не появлялось — текст резался родительским `overflow_hidden` (скрин-сверка, см. шапку `ui/text_fit.rs`). Поэтому «нет …» — валидная претензия ТОЛЬКО если подтверждена кадром; править её нужно через `ui::text_fit::fit`, а не добавлением `text_ellipsis()`.

### Претензии к закрытию (9)

- **137 sample-input** (ц.18)
  Осталось: живой `<input>` с `:focus` accent-рамкой (у нас статичный div) и UA-цвет плейсхолдера вместо зашитого text-muted. Геометрия замерена и совпала: 360.00 × 33.6, p 8/12, r-sm, bg-base.
- **138 sample-dropdown** (ц.18)
  Закрыто: пункт растягивается на ширину меню (дали явные 220 — `w_full` внутри absolute taffy не резолвил, заливка была короче на 58 px), hint выбранного пункта в светлой теме красится в `accent-action-fg`. Ждёт пиксельного подтверждения.
- **139 sample-tree** (ц.18)
  Осталось: семпл дерева статичен — у оригинала локальный `useState`, клик раскрывает папку и меняет выделение. ## Цикл 18 (доработка): DIVERGES Закрыто: семпл дерева стал интерактивным — состояние (`tree_expanded`, `tree_selected`) переехало в `DesignState`, клик по папке раскрывает её, по файлу переносит выделение (`DesignAction::TreeClick`). Ждёт пиксельного подтверждения.
- **142 sample-modal-triggers** (ц.18)
  Осталось: `bodyHtml` с разметкой `<code>`/`<strong>` — тело модалки принимает плоский текст.
- **143 sample-external-toast-triggers** (ц.18)
  Осталось: внешние тосты в отдельном окне с таймер-баром 8 с и паузой по ховеру — кнопки поднимают внутренний стек.
- **146 sample-horizontal-tab-strip** (ц.18)
  Закрыто (подтверждено кадром): Terminal и Problems — Phosphor-SVG, Output — кодикон, все 13 px. Осталось: слой `.tabs` (flex 1 / overflow-x auto), UA-шрифт `<button>`, `letter-spacing .02em`, ellipsis у лейбла.
- **151 sample-status-bar-items** (ц.18)
  Осталось: у оригинала `<button>` без `font: inherit` рисуется UA-шрифтом формы, у нас Bricolage. Рецепт пилюли 1:1.
- **156 focus-visible-ring** (ц.22)  ⚠ нет пары кадров — вердикт не подтверждён
  Механизм есть. `:focus-visible` = «фокус пришёл с клавиатуры», и в gpui этой модальности не было — вендорный патч (`window.rs`, 6 маркеров) поднимает `Window::focus_visible` на `focus_next`/`focus_prev` и гасит на любом mouse-down. Кольцо рисует `ui/focus_ring.rs`: `outline` в gpui нет, поэтому это абсолютный оверлей, вынесенный на `offset 2 + ring 2` наружу, бордер 2 px `accent-primary`, радиус э
- **157 activity-drop-highlight** (ц.18)  ⚠ нет пары кадров — вердикт не подтверждён
  Закрыто: карта остаётся подсвеченной при самодропе — `isOver` в оригинале истинно и когда `sourceSlot === slot`. Ждёт пиксельного подтверждения.

Формат ответа: `NN <имя>: MATCH|DIVERGES` + при DIVERGES 1-3 строки (оригинал файл:строка/значение → у нас файл:строка/значение → как чинить). В конце — сводка и список закрытых пунктов.

## Зона 1-19 — Титлбар: цикл 20

Порт: `crates/shell/src/{ui/titlebar.rs, ui/session_tabs.rs, ui/layout_popover.rs}`. Оригинал: `%PROJECTS%\kamin-ide\src\renderer\components/titlebar/*` + `theme/*.css`.

По КАЖДОМУ пункту ниже ответь «закрыто» или «осталось» с числом/строкой кода; новое — отдельным списком. Мерить пиксели скриптом (PIL/numpy) в СВОЮ временную папку, в `parity/` не писать. DPR обеих сторон 1.25; в наших кадрах бывает рамка захвата ~9 физ. px слева и сверху — калибруйся по ней перед выводами.

Ограничения gpui (НЕ дефект, но упоминать, если влияет): нет `letter-spacing`, нет `transform`, `svg().text_color()` не реагирует на hover, ровно ОДИН `.hover()` на элемент, у `Stateful` нет `self_center()`, `Input` берёт кегль и паддинги из своего `Size`, а не из обёртки.

Про эллипсис: `text_ellipsis()` ВЫСТАВЛЯЕТ `TextOverflow::Truncate("…")` (`vendor/gpui/src/styled.rs:83-88`), но на экране для `.child(SharedString)` многоточие не появлялось — текст резался родительским `overflow_hidden` (скрин-сверка, см. шапку `ui/text_fit.rs`). Поэтому «нет …» — валидная претензия ТОЛЬКО если подтверждена кадром; править её нужно через `ui::text_fit::fit`, а не добавлением `text_ellipsis()`.

### Претензии к закрытию (7)

- **2 -titlebar-left-cluster** (ц.19)
  СОГЛАСОВАННОЕ отклонение: обёртки `.leftCluster` нет, отступ задаёт `STRIP_PL 48` (просили больше воздуха). Замер рассинхрона с краем сайдбара — 142.8.
- **9 -titlebar-quick-action-button** (ц.19)
  Закрыто: ховер поднимает и цвет SVG (`group_hover` от группы кнопки) — `.btn:hover { color: text-primary }`. Ждёт пиксельного подтверждения.
- **11 layout-toggles-menu** (ц.19)
  Замер: 220.0 × 380.8, центр 1119.6 против центра триггера 1118.8, top = anchor.bottom + 6, кламп гуттером 8, шаг строк 32.8 — анкор по измеренному размеру закрыт. Осталось: `letter-spacing .04em` (упор gpui) и обход препятствия-вебвью (`browserObstacle`).
- **15 theme-popover-column** (ц.19)
  Равнение колонок ЗАКРЫТО кадром: обе picked-строки 176.0, шаг колонок 368. Осталось: `letter-spacing .04em` у `.colTitle` — упор gpui.
- **17 panel-icon-svg** (ц.19)
  Геометрия всех 8 слотов сверена дословно. Осталось: `stroke-width 1.2` по центру контура — у нас 1 px бордер внутрь бокса (упор gpui, видно на зуме).
- **18 session-tabs-strip** (ц.19)
  Рецепты «+», спейсера и пикера подтверждены живьём. Согласованные отклонения: `STRIP_PL 48` и кнопка «N ⌄». Осталось: `transform: scale(1.06)` (упор gpui) и структура (у оригинала «+»/спейсер/пикер — дети `.strip` с `flex: 1`).
- **19 session-tab-chip** (ц.19)
  Ядро подтверждено кадром (чип 138.4 × 28, r-md, отступы, градиенты, точка `--text-muted`). Закрыто: при `switching` пульсирует ТОЛЬКО точка, пин остаётся; бюджет метки у спящего чипа больше на 24 px (кнопки disconnect в DOM нет) + учтены 2 px рамки. Осталось: механика ширины (расчёт вместо `flex: 0 1 180px`).

Формат ответа: `NN <имя>: MATCH|DIVERGES` + при DIVERGES 1-3 строки (оригинал файл:строка/значение → у нас файл:строка/значение → как чинить). В конце — сводка и список закрытых пунктов.

## Зона 38-51 — Activity, рейлы, стрипы: цикл 22

Порт: `crates/shell/src/{ui/activity_bar.rs, ui/right_column.rs, ui/slot_panel.rs, ui/tool_picker.rs}`. Оригинал: `%PROJECTS%\kamin-ide\src\renderer\components/activity-bar/*` + `theme/*.css`.

По КАЖДОМУ пункту ниже ответь «закрыто» или «осталось» с числом/строкой кода; новое — отдельным списком. Мерить пиксели скриптом (PIL/numpy) в СВОЮ временную папку, в `parity/` не писать. DPR обеих сторон 1.25; в наших кадрах бывает рамка захвата ~9 физ. px слева и сверху — калибруйся по ней перед выводами.

Ограничения gpui (НЕ дефект, но упоминать, если влияет): нет `letter-spacing`, нет `transform`, `svg().text_color()` не реагирует на hover, ровно ОДИН `.hover()` на элемент, у `Stateful` нет `self_center()`, `Input` берёт кегль и паддинги из своего `Size`, а не из обёртки.

Про эллипсис: `text_ellipsis()` ВЫСТАВЛЯЕТ `TextOverflow::Truncate("…")` (`vendor/gpui/src/styled.rs:83-88`), но на экране для `.child(SharedString)` многоточие не появлялось — текст резался родительским `overflow_hidden` (скрин-сверка, см. шапку `ui/text_fit.rs`). Поэтому «нет …» — валидная претензия ТОЛЬКО если подтверждена кадром; править её нужно через `ui::text_fit::fit`, а не добавлением `text_ellipsis()`.

### Претензии к закрытию (7)

- **38 activity-bar-nav** (ц.21)
  Закрыто: подсветка цели при самодропе снята обратно — источник `activity-dnd.ts:69-71` обнуляет `overSlot`, значит `isOver` там false. Правка ц.18 опиралась на `useActivityDropTarget.ts:36` и была неверной. Тинт `<nav>` замерен: 4.0…52.0 = ровно 48, accent 10 % + кольцо accent 60 % с инсетом 2.
- **39 activity-tile** (ц.21)
  Закрыто: тултип берётся из ОБЩЕГО реестра (`lookup_any`) — contributed-тулы больше не остаются без подсказки. ★ СПОРНО: ревьювер утверждает, что ховер не осветляет глиф плиток рейлов и что `group_hover` на кодиконе не работает вовсе. Мой контрольный замер не состоялся — probe-hover не менял даже фон, то есть событие до элемента не дошло. Проверять РЕАЛЬНЫМ курсором (`SetCursorPos`).
- **42 activity-picker-dots-trigger** (ц.21)
  ★ СПОРНО (см. 39): ревьювер замерил, что ink «…» не меняется. Правка ц.17 через `group_hover` может быть no-op — нужен замер реальным курсором. Геометрия и кегль 18 совпадают.
- **45 activity-context-menu** (ц.21)
  Позиция 1:1 (курсор 740/542.4 → меню 650.4 = `x − w/2`, top = y). Закрыто: строка «Move to» с открытым сабменю держит accent 16 % даже под курсором (`.itemMoveTo[aria-expanded]` стоит после `.item:hover` при равной специфичности).
- **46 activity-context-submenu** (ц.21)
  Нормальный случай замерен и совпал (left = `x + MENU_W − 1`, top по центру строки, бокс 180 × 164, строка 30.4). Закрыто: флип ВЛЕВО, когда справа не влезает — раньше сабменю накрывало родительское меню.
- **49 bottom-tab** (ц.21)
  Закрыто: неизвестный id таба больше НЕ рисует фантомный «Tool» (`if (!item) return`), у таба появился тултип с лейблом тула. ★ СПОРНО: ховер-цвет подписи и глифа (см. 39).
- **51 tool-icon** (ц.21)
  Закрыто: `ActivityPlaceholder` перешёл на ЕДИНЫЙ резолв `tool_glyph_split` — свой код падал в `codicon-play` и не знал `<img>`-ветки (один тул давал «play» в теле и «file» в баре). Осталось: `data:`-иконки и `file://host/...` в img-ветке, `object-fit: contain`. Лишние Phosphor-алиасы — то же семейство, что согласованное отклонение.

Формат ответа: `NN <имя>: MATCH|DIVERGES` + при DIVERGES 1-3 строки (оригинал файл:строка/значение → у нас файл:строка/значение → как чинить). В конце — сводка и список закрытых пунктов.

## Зона 20-37 — Сайдбар: цикл 21

Порт: `crates/shell/src/{ui/sessions_list.rs, ui/customize.rs}`. Оригинал: `%PROJECTS%\kamin-ide\src\renderer\components/sidebar/*` + `theme/*.css`.

По КАЖДОМУ пункту ниже ответь «закрыто» или «осталось» с числом/строкой кода; новое — отдельным списком. Мерить пиксели скриптом (PIL/numpy) в СВОЮ временную папку, в `parity/` не писать. DPR обеих сторон 1.25; в наших кадрах бывает рамка захвата ~9 физ. px слева и сверху — калибруйся по ней перед выводами.

Ограничения gpui (НЕ дефект, но упоминать, если влияет): нет `letter-spacing`, нет `transform`, `svg().text_color()` не реагирует на hover, ровно ОДИН `.hover()` на элемент, у `Stateful` нет `self_center()`, `Input` берёт кегль и паддинги из своего `Size`, а не из обёртки.

Про эллипсис: `text_ellipsis()` ВЫСТАВЛЯЕТ `TextOverflow::Truncate("…")` (`vendor/gpui/src/styled.rs:83-88`), но на экране для `.child(SharedString)` многоточие не появлялось — текст резался родительским `overflow_hidden` (скрин-сверка, см. шапку `ui/text_fit.rs`). Поэтому «нет …» — валидная претензия ТОЛЬКО если подтверждена кадром; править её нужно через `ui::text_fit::fit`, а не добавлением `text_ellipsis()`.

### Претензии к закрытию (5)

- **23 sessions-mode-root** (ц.20)
  Осталось: `letter-spacing .08em` у «PROJECTS» — упор в gpui (заголовок ≈7 px уже). Остальное (`.actions`, `.action` с `group_hover` на глифе, `.header`, `.list`, `.empty` внутри списка) сверено.
- **24 project-group-header** (ц.20)
  Закрыто: тултип теперь только на имени группы. Осталось: «…» при усечении (`ui::text_fit::fit`) и наш RMB «Delete project» — интеракция сверх оригинала, оставлена намеренно. ## Цикл 22 (правка): DIVERGES Закрыто: «…» при усечении. Многоточие в порте дописывает `text_fit`, движок его не рисует; добавлен `fit_approx` (оценка по числу символов, ±1 символ) и бюджеты по ширине контейнера из probe прошл
- **28 session-item-row** (ц.20)
  Закрыто: активация по mouse-UP (`onClick`) и двойной клик → переименование. Осталось: «…» при усечении, F2 на СФОКУСИРОВАННОЙ строке (у нас переименовывается активная), тултип абсолютного времени. ## Цикл 22 (правка): DIVERGES Закрыто: «…» при усечении. Многоточие в порте дописывает `text_fit`, движок его не рисует; добавлен `fit_approx` (оценка по числу символов, ±1 символ) и бюджеты по ширине ко
- **31 session-rename-input** (ц.20)
  Закрыто: у `Input` погашены собственные `px 8 / py 2 / h 24` — отступ и рамку даёт обёртка `.renameInput` (padding 1/4), высота поля 16 внутри строки 24; горизонтальные кромки больше не срезаются. Ждёт пиксельного подтверждения.
- **33 session-context-menu** (ц.20)
  Закрыто: кламп по ИЗМЕРЕННОЙ коробке (замер живьём 202.4 × 177.6) вместо фиксированных 200 × 260 — промах у краёв был 4 и 81 px. Ждёт пиксельного подтверждения.

Формат ответа: `NN <имя>: MATCH|DIVERGES` + при DIVERGES 1-3 строки (оригинал файл:строка/значение → у нас файл:строка/значение → как чинить). В конце — сводка и список закрытых пунктов.

