# 126 — verdict (review cycle 1)
VERDICT: DIVERGES
SymbolKind-карта сдвинута (12=variable, 13=constant, 14=misc, 3=misc, 24=misc по
оригиналу); иконка accent-blue vs наследование; shadow как 124; нет text-right/50vh/
стрелок. reveal OpenFileAt + pt12vh — подтверждены.

## Цикл 2: DIVERGES
path text-right; max-h 50vh; baseline; стрелки.

## Цикл 5: DIVERGES

Workspace symbols: глиф без своего цвета, path справа, max-h, reveal через `OpenFileAt` — исправлены. Инпут-ряд и скрим — **волна 8**. Остаток: kind-карта слепляет 8 (constructor) и 11 (function) в глиф метода; нет стрелок, debounce 120 мс, светлого active.

## Цикл 6: DIVERGES

kind-карта слепляет 8/11 с методом; стрелки, debounce, светлый active.

## Цикл 7: DIVERGES

Глифы ctor/function/method слеплены в один; нет стрелок и debounce 120 мс; светлый active нет.

## Цикл 8: DIVERGES

Закрыто: debounce 120 мс, ↑/↓ по списку, Enter по активной строке, светлая тема —
сплошная accent-заливка активной строки (модуль CSS общий с QuickOpen).

Осталось: глифы ctor/function/method слеплены в один; живая проверка клавиатуры
probe-ом невозможна (см. 124).

## Цикл 12: DIVERGES

Закрыто: в светлой теме активная строка флипает НЕ ТОЛЬКО фон — имя красится в
`--accent-action-fg`, путь в 80% от него (парные правила `QuickOpen.module.css:66-68,80-82`,
в цикле 8 я закрыл только заливку).

Осталось: глифы symbol-method / symbol-constructor / symbol-function слеплены в один.

## Цикл 12 (добивка): DIVERGES

Закрыто: ховер = активная строка (сплошной accent в светлой теме).

Осталось: глифы symbol-method / symbol-constructor / symbol-function слеплены в один.

## Цикл 13: DIVERGES

Закрыто: скрим `--overlay-soft` (см. 124).

Осталось: kind-карта склеивает method/constructor/function в один глиф;
кегль глифа 14 против базовых 16; двойная подсветка.

## Цикл 14: DIVERGES

Закрыто: кегль kind-глифа 14 → **16** — у `<span class="codicon codicon-symbol-…">`
класса-кегля нет вовсе, работает вендорная база.

Осталось: kind-карта склеивает method/constructor/function в один глиф;
двойная подсветка (ховер + активная строка).

## Цикл 15: DIVERGES

Осталось: глиф символа должен наследовать цвет строки (светлая тема), двойная подсветка, кап 100 строк.

## Цикл 17: DIVERGES

Закрыто: центрирование и двойная подсветка.
Осталось: глиф символа на активной строке в светлой теме должен краситься в `accent-action-fg`; кап 100 строк; `backdrop-filter`.

## Цикл 20: DIVERGES

Ревьювер снял давнюю ложную претензию: `symbol-method/function/constructor` в самом шрифте — ОДИН глиф `ea8c`, у нас верно.
Закрыто: кегль ряда ввода 13.
Осталось: цвет глифа на активной строке в светлой теме, кап 100 строк.

## Цикл 23: DIVERGES

Осталось: кап `MAX_ROWS = 100` (у оригинала капа нет) и глиф активной строки в светлой теме не красится в accent-action-fg.

## Цикл 23: DIVERGES

Закрыто в этом цикле: пол 320 убран. Осталось: кап `MAX_ROWS = 100` — СОЗНАТЕЛЬНОЕ отклонение (невиртуализованный список — корневая причина фризов в этом проекте), и глиф активной строки в светлой теме не красится в accent-action-fg.

## Цикл 23: DIVERGES

Закрыто в этом цикле: глиф активной строки в СВЕТЛОЙ теме красится accent-action-fg вместе с именем — цвет прокинут в codicon, как наследование от `[data-theme=light] .itemActive`. Осталось: кап `MAX_ROWS = 100` — сознательное отклонение ради перф-бюджета (невиртуализованный список = корневая причина фризов проекта).

## Цикл 26: DIVERGES

Карта глифов сверена поглифно и верна — устарел текст `ours.md`, а не код.

Осталось: `MAX_ROWS = 100` — у оригинала капа нет ни на одном этаже (рендерер,
exthost, мост), сто первый символ не отрисуется и стрелкой до него не дойти;
сознательное перф-отклонение, нужно решение
