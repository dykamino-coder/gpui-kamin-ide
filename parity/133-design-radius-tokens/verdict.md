# 133 design-radius-tokens — вердикт ц.15

Расхождение: `.radiusGrid` — `grid auto-fill minmax(120px,1fr)` gap 12, у нас
жёсткая `w(120)`; центры боксов расходились до 41.7 px.

Правка: `div().grid().grid_cols_min(px(120)).gap(12)`, ширина у ячейки снята.

Статус: **MATCH** (после перепроверки дорожек замером).

## Цикл 15: MATCH

## Цикл 18: MATCH

Радиусы: 12 дорожек, шаг 137.6/138.4 при теории 137.93, бокс 80 центрирован в дорожке.
