

## Цикл 4: DIVERGES

Тулбар заголовка: кнопки 22×22/глиф 14/ховер bg-surface 60%/disabled 0.4 и
глифы (EBF8/EAC5/EB37) — MATCH. Locate считал 24px на строку при фактических
22 — **исправлено волной 7** (`root.rs` scroll-to-row 22.0).
Остаётся: Collapse не тумблер, `disabled` не вычисляется, нет `.flash`
(treeFlash 0.9s), Refresh — пере-листинг вместо ремаунта, нет тултипа
«Building the search index…». `letter-spacing 0.08em` — ограничение gpui.

## Цикл 8: DIVERGES

Шаг скролла Locate 22 закрыт. Осталось: Collapse не тумблер, `disabled` не вычисляется, нет `.flash`, Refresh — пере-листинг вместо ремаунта, нет тултипов Indexing и полного пути у титула.

## Цикл 10: DIVERGES

Титул pl 12 и питч кнопок 24.4 совпали. ОСТАЛОСЬ: Collapse не тумблер (всегда collapse-all, оригинал меняет глиф и лейбл по treeAllCollapsed); disabled не вычисляется у трёх кнопок; нет .flash; Refresh = пере-листинг вместо полного ремаунта; нет тултипов у титула (полный путь) и бейджа Indexing.

## Цикл 11: DIVERGES

Закрыто: Collapse стал ТУМБЛЕРОМ — глиф `collapse-all` ↔ `expand-all` и подпись
«Collapse all folders» ↔ «Expand all folders» по `treeAllCollapsed`; кнопки
блокируются как в оригинале (Locate — без воркспейса или без выделения,
Collapse/Refresh — без воркспейса, `opacity .4`, тултип остаётся); у титула появился
тултип с полным путём воркспейса, у бейджа — «Building the search index (Ctrl+P)…».

Осталось: Refresh перечитывает листинги, а не ремаунтит дерево целиком; нет `.flash`;
Expand-All раскрывает уровни с уже известным листингом и запрашивает следующий (кап
2000 директорий), тогда как оригинал каскадит по мере монтирования.

## Цикл 13 (ревью зоны): DIVERGES

Ревью подтвердило метрики хедера целиком (pl 12 / pr 8 / py 8, gap 4/2,
кнопка 22×22 r-xs + hover bg-surface 60 %, глиф 14 — тут класс задан через
`:global(.codicon)` и ПОБЕЖДАЕТ базу, Indexing 12, ss01 + weight 500,
тумблер collapse/expand, все кодпоинты).

Осталось: Refresh у нас пере-листывает раскрытые директории, а оригинал
полностью ремаунтит дерево (узлы теряют `childCap`/`entries`); `.flash` и
плавный `scrollIntoView`; поллинг `[data-tree-id]` при Locate; disabled
Locate завязан на выделение дерева, а в оригинале — на активный файл
редактора.

## Цикл 14: DIVERGES

Закрыто: Locate завязан на АКТИВНЫЙ ФАЙЛ РЕДАКТОРА — и цель, и `disabled`.
`FileTreeHeader` берёт `selectedFile.value` (сигнал файл-вьюера), а у нас
приоритет был обратный: сначала выделение дерева, таб — фоллбек; кнопка
гасла по пустому выделению дерева, хотя открытый файл был.

Проверено на живом окне: при открытом табе кнопка активна.

Осталось: Refresh у нас пере-листывает раскрытые директории, а оригинал
полностью ремаунтит дерево; `.flash` и плавный `scrollIntoView`; поллинг
`[data-tree-id]` перед скроллом; `letter-spacing .08em` (движок).

## Цикл 15: DIVERGES

Осталось: `letter-spacing .08em` и «…» у заголовка, вращение спиннера Indexing, плавный Locate с `.flash`.

## Цикл 18: DIVERGES

Закрыто: `.btn[disabled] { cursor: not-allowed }`.
Осталось: `letter-spacing .08em` и «…» у титула, вращение спиннера Indexing, полный ремаунт по Refresh, Locate с центрированием + `.flash` + ретраем.

## Цикл 21: DIVERGES

Закрыто: `cursor: not-allowed` у disabled (вердикт ц.18 был преждевременным — правка не применилась) и ховер кнопки красит глиф.
Осталось: `letter-spacing`, «…» у титула, вращение спиннера Indexing, полный ремаунт по Refresh, Locate с центрированием и `.flash`.

## Цикл 22 (правка): DIVERGES

Закрыто: Locate центрирует строку в видимой области
(`scrollIntoView({block:"center"})`) — высота тела берётся из probe, вместо
фиксированных 140 px от верха; при отсутствии листингов предков ставится
повтор через 120 мс (аналог поллинга `[data-tree-id]` 50 мс ×60), раньше
скролла просто не происходило.

Осталось: `.flash` 0.9 с после Locate, плавность `behavior: smooth`,
`letter-spacing` и «…» у титула, вращение спиннера Indexing, полный
ремаунт дерева по Refresh.

## Цикл 22 (правка 2): DIVERGES

Закрыто: `.flash` после Locate — вспышка строки 0.9 с (accent 40 % → прозрачность, `treeFlash 0.9s ease-out 1`); ключ анимации содержит счётчик, поэтому повторный Locate по тому же файлу перезапускает вспышку.
Осталось: `letter-spacing` и «…» у титула, вращение спиннера Indexing, полный ремаунт дерева по Refresh.

## Цикл 22 (правка 3): DIVERGES

Закрыто: кнопка Refresh делает ПОЛНЫЙ ремаунт (`RefreshTreeHard`) — кэш
листингов и капы детей сбрасываются, раскрытые папки снова показывают
«Loading…», как `workspaceFolder = null → микротаск → назад`
(`FileTreeHeader.tsx:64-71`). Watcher по-прежнему шлёт мягкий `RefreshTree`
без сброса кэша — иначе панель мигала бы на каждое событие ФС.

Осталось: `letter-spacing` и «…» у титула, вращение спиннера Indexing.

## Цикл 22: DIVERGES

Закрыто: спиннер Indexing вращается. Глиф `codicon-loading` вынут из
`codicon.ttf` в SVG-ассет (`icons/codicon-loading.svg`) и крутится
`with_transformation(rotate)` + `with_animation`, кадры дискретные —
`steps(30)` за 1.5 с, как `codicon-modifier-spin` (`codicon.css:35-40`).

Осталось: `letter-spacing .08em` у заголовка (в gpui межбуквенного интервала
нет — движковый предел).

## Цикл 23: DIVERGES

Спиннер Indexing вращается (30 шагов / 1500 мс = `codicon-modifier-spin`). Осталось `letter-spacing .08em` — упор движка.

## Цикл 25: DIVERGES

Закрыто в этом цикле:
- «…» у титула: `text-overflow: ellipsis` (`FileTreeHeader.module.css:16-17`)
  теперь дописывает `fit_approx` по ширине панели минус паддинги 8+12, три
  кнопки 22 и гэпы — раньше строка просто обрезалась (движок «…» не рисует,
  для строк дерева это чинили, для титула нет);
- спиннер Indexing: период/кривая — см. 92 (общая правка `ui/icon.rs`).

Осталось: `letter-spacing .08em` — предела движка (поля нет в `TextStyle`
вендорного gpui); `transition: background/color 150ms` у кнопок — тоже упор;
плавный `scrollIntoView({behavior:"smooth"})` у Locate (у нас мгновенный
`set_offset`, центрирование верное).

Условная находка (сегодня НЕ расхождение): предикат `disabled` у Locate у нас
`!editor_tabs.is_empty()`, у оригинала `!selectedFile.value`. Пока каждый таб
редактора — файл, это одно и то же; разойдётся, когда появятся вебвью-табы
(досье 114). Привязано к 114, отдельной правкой не трогаю.

## Цикл 32: DIVERGES

`letter-spacing` БОЛЬШЕ НЕ УПОР ДВИЖКА — трекинг реализован вендорным патчем
(план 99): `shape_text_spaced` → `layout_wrapped_line_spaced` →
`layout_line_spaced` → сдвиг глифов рядом с проходом `force_width`, плюс
`TextStyle.letter_spacing` и `Styled::letter_spacing()`. Значение проставлено
в этом элементе.

Проверено новой probe-командой `shape` (шейпит строку ТЕМ ЖЕ шейпером, что и
рендер — пиксельный поиск по кадру трижды ловил соседнюю строку):
«NOTIFICATIONS» 83.2 → 91.78 при spacing 0.66 (13 символов × 0.66 = 8.58),
«TERMINAL» 54.99 → 60.27 (8 × 0.66 = 5.28). Прирост совпадает с моделью
глиф-в-глиф на обеих строках.

Осталось: сверить АБСОЛЮТНУЮ ширину с оригиналом тем же методом. Эталон 89.6
из прошлых циклов снят с КАДРА (ink extents), а не шейпером, и сравнивать его
с 91.78 нельзя — методы разные. Нужен прогон настоящего KaminIDE с той же
командой `shape`

## Цикл 34: MATCH

Упор «сверить АБСОЛЮТНУЮ ширину с оригиналом» снят: оригинальный KaminIDE
поднят с CDP (`KAMIN_EXE=…/debug/kaminide.exe CDP_PORT=9223 node
scripts/launch-cdp.mjs`; release-сборка отладочный порт НЕ открывает), и
метрики сняты прямо из его DOM новым скриптом `scripts/orig_measure.mjs`
(`Runtime.evaluate` по WebSocket, без зависимости `ws` — глобальный WebSocket
Node 22).

Сверка «строка → ширина» оригинал / наш `probe shape` (тот же шейпер, что и
рендер):

| строка | 11px | вес | трекинг | оригинал | наш | Δ |
|---|---|---|---|---|---|---|
| PROJECTS | 11 | 500 | 0.88px (.08em) | 59.73 | 59.46 | 0.27 |
| CONSOLE | 11 | 400 | 0.44px (.04em) | 53.14 | 53.17 | 0.03 |
| CLAUDE | 11 | 400 | 0.44px | 44.66 | 44.64 | 0.02 |
| PLAN | 11 | 400 | 0.44px | 29.77 | 29.47 | 0.30 |
| Terminal | 11 | 500 | 0.22px (.02em) | 46.50 | 46.38 | 0.12 |

Расхождение ≤0.3px на всех пяти строках и на трёх разных значениях трекинга —
это округление раскладки браузера, а не разница шейпинга. Прежний «эталон
89.6» был снят с КАДРА (ink extents) и к сравнению не годился вовсе.

Значение трекинга у этого элемента взято из CSS оригинала напрямую (список
всех 24 правил `letter-spacing` вытащен grep-ом по `src/renderer/**/*.css`).
