# Кадры: N/A

Элемент визуального представления НЕ ИМЕЕТ — сверять нечего, кадры обеих
сторон отсутствуют законно.

`:focus-visible { outline: 2px solid var(--accent-primary); outline-offset: 2px }`
— состояние клавиатурного фокуса. В порте не реализовано вовсе (grep по
`outline|focus_visible` в `crates/shell/src` пуст), у оригинала это правило
`global.css:34-43`. Кадр оригинала имеет смысл снять ТОЛЬКО вместе с
реализацией — иначе сравнивать не с чем.
