# 69 — verdict (review cycle 1)
VERDICT: MATCH
36/text-disabled, fs-md 600, fs-xs max-w 240, без пикера — 1:1 (lh дефолт — допуск).

## Цикл 5: MATCH

Activity-плейсхолдер реализован: глиф 36 text-disabled, label fs-md/600 primary, hint fs-xs max-w 240, без пикера. Нет `lh-snug` у хинта. (`ours.md` устарел — там значилось «не реализовано».)

## Цикл 6: MATCH

Activity-плейсхолдер 1:1; у хинта нет `lh-snug` (наследует глобальные 1.169).

### Ц.13 — снят кадр нашей стороны

`ours.png` наконец есть (правая верхняя карта в состоянии выбранной активности
без тела). Вердикт по существу не пересматривался — сверка ждёт кадра
оригинала.

## Цикл 14: DIVERGES

Закрыто: codicon-ветка глифа — эффективный кегль **16**, а не 36:
`.glyph{font-size:36px}` стоит на самом `<i class="codicon …">` и проигрывает
вендорной базе. У svg-ветки 36 задан атрибутами `width/height` и остаётся.

Осталось: `stroke-width: 1.2` рамки против нашего целого бордера.

## Цикл 16: MATCH

ActivityPlaceholder: svg 36 / кодикон 16 (правило `.glyph{36px}` на самом `.codicon` проигрывает вендорной базе), label fs-md/600, hint max-w 240. Кадр оригинала снят под CDP.

## Цикл 19: MATCH

ActivityPlaceholder: svg 36 / кодикон 16 по каскаду.
