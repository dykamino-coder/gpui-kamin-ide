
## Цикл 3: DIVERGES

Пин: ховер строки должен давать opacity 0.7, свой ховер — цвет tab-color БЕЗ фона; у нас фон text-primary 12%. Волна 7.

## Цикл 4: DIVERGES

Пин рисовал прямоугольный фон text-primary 12% и появлялся сразу с opacity 1 — **исправлено волной 7**: `.pin:hover { color: tab-color }` без фона, показ с opacity 0.7 → 1 по своему ховеру.

## Цикл 8: MATCH

Пин 1:1: без фона, `invisible → visible + 0.7` по ховеру строки, свой ховер 1.0, tab-color, fa 10 в боксе 14.

## Цикл 13: MATCH

Закрыто последнее расхождение — `display: none` вместо `visibility: hidden`
(см. 28). Метрики (20×20, r-xs, глиф 10, цвета и тултипы) ревью подтвердило.

## Цикл 16: MATCH

Пин 20×20, скрыт с НУЛЕВОЙ шириной (эквивалент `display:none`), `group_hover` → .7, свой ховер → 1 + цвет вкладки; глиф `fa-thumbtack` 10.

## Цикл 20: MATCH

Пин 20×20 r-xs, `fa-thumbtack` 10, скрыт с нулевой шириной, `group_hover` → .7, свой ховер → 1 + цвет вкладки.
