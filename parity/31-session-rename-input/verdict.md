
## Цикл 3: DIVERGES

Rename-инпут: нужен bg-base + бордер accent + radius 4 + padding 1/4 и коммит по blur. Волна 7.

## Цикл 4: DIVERGES

Rename-инпут без фона/рамки/паддинга (`appearance(false)`), строка без `bg-surface`, коммита по blur и select-all при входе нет. Оригинал: bg-base + 1px accent-primary + radius-xs + padding 1/4. Волна 8.

## Цикл 8: DIVERGES

Rename-инпут без рецепта: нужен bg-base + 1px accent + radius-xs + padding 1/4, `.editing { background: bg-surface }`, коммит по blur, select-all.

## Цикл 9: DIVERGES

Не тронуто: `.editing{background:bg-surface}` + инпут bg-base, 1px accent-primary, radius-xs, padding 1/4 (`SessionItem.module.css:174-187`), select-all и `onBlur=commit` (`SessionItem.tsx:41,82`) против `Input::new(input).appearance(false)` без фона/рамки/паддинга (`sessions_list.rs:246-266`) и создания без select/blur (`root.rs:4193-4199`). Чинить: обёртка + `cx.subscribe` на `InputEvent::Blur` + `dispatch_action(SelectAll)`.

## Цикл 13: DIVERGES

Закрыто: поле переименования получило рецепт оригинала — фон `bg-base`, рамка
1px `accent-primary`, radius-xs, padding 1/4, кегль fs-sm; строка в режиме
редактирования красится `bg-surface` (`.editing`). Раньше это был голый
`appearance(false)` без фона, рамки, паддинга и кегля.

Осталось: коммит по blur; выделение всего текста при входе в режим.

## Цикл 13 (добивка): DIVERGES

Закрыто: коммит по blur (`onBlur → commit`, `SessionItem.tsx:82`) — подписка на
`InputEvent::Blur` сохраняет имя так же, как Enter. Раньше уход фокуса оставлял
строку в режиме правки навсегда.

Осталось: выделение всего текста при входе в режим (`select()` оригинала).

## Цикл 13: DIVERGES

Закрыто: при входе в правку имя выделяется целиком (`ref.select()`
оригинала) — раньше курсор просто вставал в конец.

ВАЖНО про предыдущий отчёт ревью: три его пункта по этому элементу оказались
неверны — стили инпута (bg-base, рамка accent-primary, radius-xs, padding 1/4,
кегль fs-sm), фон `.editing` = bg-surface и `onBlur → commit` в коде УЖЕ есть
(`ui/sessions_list.rs`, `root.rs`), проверено чтением файлов.

Осталось: dblclick по строке как второй вход в правку.

## Цикл 14: DIVERGES

Ревью независимо подтвердило, что три пункта прошлого отчёта были ложными:
стили инпута, фон `.editing` и `onBlur → commit` в коде уже были. `SelectAll`
при входе — тоже на месте.

Новая настоящая находка: `gpui_component::Input` поверх нашей обёртки ставит
СВОИ `px 8 / py 2 / h 24`, поэтому текст начинается в 12px от края вместо 5, а
высота поля фиксирована 24 внутри строки 22.

## Цикл 16: DIVERGES

Закрыто: `theme.colors.foreground` синхронизирован с `--text-primary` — текст инпута переименования больше не крейтовый `#fafafa`.
Проверить кадром в состоянии rename.

## Цикл 20: DIVERGES

Закрыто: у `Input` погашены собственные `px 8 / py 2 / h 24` — отступ и рамку даёт обёртка `.renameInput` (padding 1/4), высота поля 16 внутри строки 24; горизонтальные кромки больше не срезаются.
Ждёт пиксельного подтверждения.

## Цикл 23: DIVERGES

Rename-инпут подтверждён пикселями (рамка строки цела, фон `#313240`, левый край 92.8 = pl16+dot4+gap8, select-all и Esc работают), но поле выше оригинала на ~2.5-3 лог.: `.h(px(16.0))` у `Input` не отрабатывает, внутренняя коробка ≈17.6-18.8 против 19-20 по CSS.

## Цикл 26: DIVERGES

Осталось: высота поля 20 против ~18.4 у оригинала; каскад `.editing` в СВЕТЛОЙ
теме — активная строка в правке должна сохранять градиент 42/26, у нас
безусловный `bg-surface`; `.active:hover` над строкой в правке возвращает
active-градиент, у нас остаётся `bg-surface`

## Цикл 35: DIVERGES

Каскад `.editing` разобран и починен. Правило объявлено ПОЗЖЕ `.row`,
`.tinted` и `.active` (`SessionItem.module.css:174`), поэтому в тёмной теме
перекрывает их фон — это у нас и было. Но специфичность у него 0,1,0, а у
светло-тематических `:global([data-theme="light"]) .active|.tinted` и у всех
ховер-правил (`.row:hover`, `.tinted:hover`, `.active:hover`) — 0,2,0. Значит
в СВЕТЛОЙ теме активная и цветная строки в правке сохраняют свой градиент, а
под курсором градиент возвращается в любой теме.

Теперь `bg-surface` ставится только когда `!(light && (active || tinted))`;
ховер-стили уже висят на строке своим `.hover()` и перекрывают базу сами.

Высота поля: НЕ закрыта. Попытка зафиксировать коробку 18.4 (padding 1/1 +
рамка 1/1 + строка 14.4) срезала нижние выносные — вендорный `Input` рисует
строку со своим вертикальным сдвигом, и при `h(14.4)` глифы уходят под нижнюю
кромку (проверено кадром дважды: с `h` и с `h + line_height`). Откатил: внешняя
коробка снова 20 против 18.4 у оригинала, зато текст не режется.

Осталось: высота поля 20 против 18.4 — вендорный `Input` не отдаёт свою
вертикальную метрику, лечится только патчем вендора (тот же узел, что сломал
панель поиска в ц.30)
