# 07 — verdict (review cycle 1)
VERDICT: MATCH
.btn 36×36 r50% + devtools + close-hover red — 1:1 (глиф 13 подтверждён).

## Цикл 2: MATCH

## Цикл 8: MATCH

**Глиф 16 подтверждён пиксельно**: ink max/close 11.20 у обеих сторон, штрих min 1.60. Каскад оригинала объяснён: `.codicon[class*=codicon-]` (0,2,0) бьёт `.btn > i {13px}` (0,1,1). Остаток: центр на 1 физ. px ниже (растеризация).

## Цикл 11: MATCH

ink close 11.20 лог.

## Цикл 15: MATCH

Кнопка титлбара 36×36 круг, глиф 16 (вендорная база бьёт `.btn > i`), fa-bug 13, `.devtools` r12 + подпись, `.close:hover` accent-red.

## Цикл 19: MATCH

Кнопка титлбара: кодиконы 16 подтверждены ink-замерами (штрих 1.6, close 11.2², max 12.8²). Закрыто: бокс `fa-bug` пиннится к 16×16 правилом `.btn > i` (0,1,1), которое бьёт FA-шное `width: 1.25em`.
