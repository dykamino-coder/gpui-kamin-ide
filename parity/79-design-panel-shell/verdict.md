# 79 — verdict (review cycle 1)
VERDICT: DIVERGES
Нет .sectionBody (border bg-surface60% + bg-mantle + r12 + p16); sectionTitle 13
vs fs-lg 16; subtitle 11 vs fs-sm 12 (без lh-snug); секции mb20 vs gap24 + pb24.
(Витрины токенов ПЕРЕПИСАНЫ в wave2 — shell-стили карточек ещё нет.)

## Цикл 5: DIVERGES

Design-панель: нет `.sectionBody` (border 1px bg-surface 60% + radius-md 12 + bg-mantle + padding 16) — тело секции голое; `sectionTitle` 13 вместо fs-lg 16, `sectionSubtitle` 11 вместо fs-sm 12 + lh-snug; ритм `mb 20` вместо `.root { gap 24; padding-bottom 24 }`, нет `gap 12` в секции и `gap 2` в хедере.

## Цикл 6: MATCH

**Закрыто волной 9**: карточка `.sectionBody`, заголовок fs-lg/600, сабтайтл fs-sm + lh-snug, `gap 12`/`gap 2`, ритм 24.

## Цикл 16: DIVERGES

Гриды после правки ц.15 подтверждены живым кадром (свотчи 180 / радиусы 120 / тени 140).
Осталось: корень панели скроллит сам (вложенный скроллер, полоса на 24 px левее), ритм секций через `mb` вместо `gap` корня.

## Цикл 19: DIVERGES

Осталось: скроллер — у оригинала скроллит `.body` панели Customize, у нас `design-panel` скроллит сам внутри уже скроллящегося тела (полоса на 24 px левее); ритм секций через `mb` вместо `gap` корня.

## Цикл 23: DIVERGES

Закрыто в этом цикле: свой скроллер убран (страница скроллится телом Customize), ритм секций переведён с `mb` на `gap` корня + `padding-bottom`, как `.root`. Требует подтверждения кадром.

## Цикл 23: MATCH

Закрыто: своего скроллера у design-панели нет (страница скроллится телом Customize), ритм секций — `gap: space-6` + `padding-bottom` на корне, как `.root`. ★ По пути найдена и снята СВОЯ регрессия: `size_full` на корне внутри скролл-контейнера прибивал высоту к вьюпорту, и низ страницы становился недостижим — проверено прокруткой до последней секции.
