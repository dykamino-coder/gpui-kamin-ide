# 70 — verdict (review cycle 1)
VERDICT: DIVERGES
Скелет не реализован: нет .wrap (inset0, p 16/18, bg-surface), pill 84×22 + search
h22, 6 строк icon 30×30 + линии 11/9 op.6 (90/70/80/60/75/50%), шиммер 1.25s,
waitNote c секундами и attempts, sr-only. Сейчас — центрированное «Loading…».

## Цикл 5: DIVERGES

Скелет загрузки вебвью не реализован (grep `shimmer|Waiting for the extension host` = 0): нет `.wrap` inset0/p16-18/bg-surface, pill 84×22, search h22, шести строк icon 30×30 + линии 11/9 op .6 (90/70/80/60/75/50%), анимации 1.25s, waitNote с секундами и попытками, sr-only. Вместо — центрированное «Loading…». Кадра нет.

## Цикл 6: DIVERGES

Скелет загрузки вебвью не реализован.

## Цикл 13: DIVERGES

РЕАЛИЗОВАН с нуля (`ui/webview_skeleton.rs`). Раньше грузящаяся панель рисовала
плейсхолдер ПУСТОГО слота — «Open new tool or drag-n-drop tool from other
panels» — то есть прямо врала о своём состоянии.

Портированы: обёртка absolute inset-0 px 18 / py 16 gap 14 на bg-surface;
бар «пилюля 84×22 + растянутый поиск 22»; шесть строк «иконка 30×30 + линия 11
+ dim-линия 9 при opacity .6», ширины 90/70/80/60/75/50 % и коэффициент .62;
шиммер 1.25 с ease-in-out; подпись после 3 с с секундами и номером попытки.

Снято на живом окне (`ours.png`): скелет виден в трёх панелях сразу после
пина тула, до ответа extension host.

Осталось (ограничения движка): бегунок двигается через `left`, а не
`translateX`; три стопа градиента собраны из двух двухстоповых половин;
нет `role="status"`/`srOnly`. Нет кадра оригинала для попиксельной сверки —
состояние переходное и на той стороне.

## Цикл 13 (добивка): DIVERGES

По находкам ревью: пилюля 84×22 и квадрат-иконка 30×30 были ПЛОСКИМИ — теперь
это такие же `.sk`-примитивы с шиммером; блик идёт во всю ширину примитива
(`::after { inset: 0 }`, ход `translateX(-100 % → 100 %)`), а не полосой 40 %.

Гейт состояния исправлен: скелет включается по ОТСУТСТВИЮ html
(`ContributedContainerBody.tsx:87`), а не только по `!alive` — случай «вью
живой, html ещё не пришёл» падал в плейсхолдер ПУСТОГО слота.

Осталось: `tabular-nums` у подписи — в gpui фичи шрифта задаются на уровне
окна/TextStyle, у элемента их нет; кадра оригинала для попиксельной сверки нет.

## Цикл 14: DIVERGES

Ревью сверило скелет посвойственно с `WebviewLoadingSkeleton.module.css` и
подтвердило ВСЁ: обёртку, бар, строки, `.sk`-примитивы, ширины 90/70/80/60/
75/50 % и коэффициент .62, шиммер 1250 мс ease-in-out, порог 3 с, текст
подписи и её метрики.

Снят прежний пункт «перегиб градиента кусочно-линейный»: CSS-градиент
`transparent → C → transparent` тоже кусочно-линейный, расхождения нет.

Осталось: `font-variant-numeric: tabular-nums` у подписи — в gpui фичи шрифта
задаются на уровне окна, у элемента их нет, и секунды слегка дёргают ширину.

## Цикл 16: DIVERGES

Скелет достаётся только динамическим contributed-вью: встроенные (чат, консоль, todos, plan) идут через `webview_body` и рисуют свою заглушку «Loading…». Разметка самого скелета — 1:1.

## Цикл 19: DIVERGES

Закрыто: встроенные вью больше не идут мимо скелета — `webview_panel`/`webview_body` оказались мёртвыми и удалены, всё идёт через `webview_body_dyn`.
Осталось: contributed-страница Customize рисует голое «Loading…» вместо скелета.

## Цикл 19 (доработка): DIVERGES

Закрыто: contributed-страница Customize показывает ТОТ ЖЕ скелет загрузки, что и панели (было голое «Loading…»). Мёртвые `webview_panel`/`webview_body` удалены.
Ждёт подтверждения кадром.

## Цикл 23: DIVERGES

Скелет достаётся и contributed-странице Customize, мёртвых `webview_panel/webview_body` нет. Осталось: там он зовётся с захардкоженными `(0, 1)` — подписи «Waiting … · Ns» и «attempt N» не появятся никогда.

## Цикл 23: MATCH

Закрыто в этом цикле: скелет на contributed-странице Customize получает реальные секунды и попытки резолва вместо захардкоженных (0,1) — подписи «Waiting … · Ns» и «attempt N» теперь появляются.
