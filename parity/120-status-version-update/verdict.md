# 120 — verdict (review cycle 1)
VERDICT: DIVERGES
Нет downloading (progressFill 120ms + indeterminate + Updating N%); пилюля и бренд
одновременно vs взаимоисключаемость; idle-бренд не кликабелен (нет Check for updates);
hover op.85 vs accent34%; тексты тултипов. Пилюля-рецепт (22%/600/r-xs) — 1:1.

## Цикл 2: DIVERGES
Нет downloading; пилюля+бренд одновременно; idle-бренд не кликабелен.

## Цикл 5: DIVERGES

Версия/обновление: пилюля 22%/600/r-xs/hover 34% + тултип — 1:1. Остаток: нет состояния downloading (progressFill accent 32%, width 120ms linear, «Updating N%»/МБ, role=progressbar); пилюля и бренд рисуются одновременно вместо трёх взаимоисключающих; idle-бренд не кликабелен («Check for updates»).

## Цикл 6: DIVERGES

Нет состояния загрузки обновления; пилюля и бренд рисуются одновременно; бренд не кликабелен.

## Цикл 7: DIVERGES

Осталось только состояние загрузки. ДВЕ ЛОЖНЫЕ претензии: пилюля и бренд взаимоисключающи; бренд кликабелен с тултипом.

## Цикл 14: DIVERGES

Закрыто: ховер бренда поднимает цвет текста до text-primary (`.item:hover`
с специфичностью 0,1,1 перебивает `.brand` 0,1,0); у пилюли апдейта gap 4
вместо 3 и убран `py 1` (`.item { padding: 0 8 }`).

Осталось: состояние `downloading` с прогресс-полосой; клик открывает внешний
браузер вместо in-app `installUpdate()`.

## Цикл 15: DIVERGES

Закрыто: глиф `cloud-download` (eac2).
Осталось: состояние downloading с прогресс-заливкой и установка апдейта в приложении.

## Цикл 20: DIVERGES

Вердикт ц.14 по ховеру бренда снят и закрыт заново через `group_hover`.
Осталось: состояние `downloading` с прогресс-заливкой и установка апдейта в приложении.

## Цикл 22 (правка): DIVERGES

Закрыто: клик по пилюле ставит апдейт ВНУТРИ приложения (`updater::install`:
скачивание через `ureq` с отчётом прогресса → запуск инсталлятора `/S` →
выход), в пилюле состояние `downloading` — заливка `accent-primary 32 %`
шириной по проценту (при неизвестном размере полная заливка `opacity .5`,
как indeterminate), подпись «Updating N%» / «Updating N.N MB» поверх заливки,
повторный клик во время установки игнорируется, ошибка гасит полосу и шлёт
sticky-тост «Update failed: …».

Осталось: проверить на реальном апдейте (в dev-сборке канал обновлений не
опрашивается — нужен `KAMIN_GPUI_UPDATE_URL`).

## Цикл 23: DIVERGES

Прогресс-заливка и установка внутри приложения закрыты и подтверждены (заливка accent 32 %, indeterminate 100 %/0.5, лейбл поверх, ureq → инсталлятор `/S`, повторный клик заблокирован; ховер бренда #89b4fa → #cfd4e2). Осталось: гэп 6 вместо 4 в состоянии update-available (враппер `progressLabel` нужен только при закачке), тултип «Installing the update…» против «Downloading the KaminIDE update…», и пилюля остаётся кликабельной во время закачки.

## Цикл 23: MATCH

Закрыто в этом цикле: гэп 4 вне закачки (враппер `progressLabel` с гэпом 6 — только при закачке), тултип «Downloading the KaminIDE update…», во время закачки пилюля некликабельна. Прогресс-заливка и установка внутри приложения подтверждены ранее.
