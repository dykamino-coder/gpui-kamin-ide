# 45 activity-context-menu — вердикт ц.15

Расхождение: `left = x` вместо `left = x − w/2` (оригинал даёт clamp'у нулевой
якорь в курсоре при `side: "bottom"` — меню центрируется по курсору), и не
было флипа вверх при нехватке места снизу.

Правка: центровка по курсору + флип вверх (`y − h`), горизонтальный кламп
гуттером 8.

Статус: правка на месте, **ждёт пиксельного подтверждения**.

## Цикл 15: DIVERGES

## Цикл 17: MATCH

Замер: курсор (387,63) → меню left 297.6 = `x − w/2`, top 63.2; у правого края кламп на `vw − 180 − 8`. Кодпойнты и accent 16 % у «Move to» — 1:1.

## Цикл 21: DIVERGES

Позиция 1:1 (курсор 740/542.4 → меню 650.4 = `x − w/2`, top = y).
Закрыто: строка «Move to» с открытым сабменю держит accent 16 % даже под курсором (`.itemMoveTo[aria-expanded]` стоит после `.item:hover` при равной специфичности).

## Цикл 23: MATCH

Строка «Move to» с открытым сабменю держит accent 16 % под курсором; позиция `x − MENU_W/2`, `top = y` при `MENU_OFFSET_PX = 0`.
