# 76 — verdict (review cycle 1)
VERDICT: DIVERGES (архитектурно, осознанно)
Composition visual hosting вместо DOM-слоя z5 + rect-копирования; нет шторки
140ms; czShared — один вебвью на Customize-страницы.

## Цикл 5: DIVERGES

Осознанно архитектурно: composition hosting + постоянные WebView2-чайлды вместо DOM-слоя z5 с копированием rect/borderRadius. Шторки чата (opacity 140 мс) нет; `czShared` — один вебвью на все Customize-страницы.

## Цикл 6: DIVERGES

Слой вебвью — composition hosting (осознанно), шторки чата нет.

## Цикл 16: DIVERGES

Осталось: (1) накрывашка чата (см. 72); (2) радиус якоря теперь берётся по зоне в масках, но GDI-регион overlay всё ещё жёсткий `RADIUS_MD`.

## Цикл 19: DIVERGES

Осталось: накрывашка чата (см. 72). Снято: расхождение по GDI-региону — его наполняет только модалка, и её радиус совпадает.

## Цикл 22 (правка): DIVERGES

Закрыто: накрывашка переключения сессии портирована (`ui/chat_switch_skeleton.rs`) — brand 96, glow 150 (запечённый спрайт, спад 66 %, «дыхание» через размер + opacity), логотип 64 с «парением» через `top`, подпись «Loading conversation…» fs-sm text-muted, полоса 180×3 r999 с бегунком из двух градиентных половин (1.15 с). Показывается поверх чат-вью, пока `switching_to` не сброшен.
Осталось: `filter: drop-shadow` у логотипа и `letter-spacing .01em` — упоры в gpui.

## Цикл 23: DIVERGES

Накрывашка смонтирована. Осталось то же, что у 72: условие показа, таймаут и фейд.

## Цикл 23: MATCH

Слой накрывашки: условие показа, таймаут и фейд приведены к оригиналу вместе с элементом 72.
