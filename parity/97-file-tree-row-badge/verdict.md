

## Цикл 4: MATCH

Badge декорации: `ml auto + pl 6 + weight 600 + fs-xs`, цвет из ThemeColor.
Нит цикла 4 (фолбэк `text_muted` вместо наследования цвета строки) —
**исправлен волной 7** (`when_some(color)`).

## Цикл 8: MATCH

Badge декорации 1:1.

## Цикл 10: MATCH

Бейдж строки 1:1: flex-shrink 0 / ml auto / pl 6 / fs 11 / weight 600 = css:147-153. Живьём правый край бейджа на 7-8 px левее края кнопки Refresh — ровно разница инсетов 15 против 8.

## Цикл 14: MATCH

Закрыто последнее: у бейджа появился `data-tooltip` из декорации. Для этого
`Deco` получил поле `tooltip`, оно парсится из payload
`FileDecorationProvider` и используется и в лейбле строки, и в бейдже.

Метрики ревью подтвердило: `ml auto`, `pl 6`, fs-xs 11, weight 600, цвет
только при ThemeColor (иначе наследование).

## Цикл 15: DIVERGES

Закрыто: бейдж без значения больше не рендерится (ел `gap 6`).
Проверить замером край лейбла.

## Цикл 18: MATCH

Бейдж: `ml auto`, `pl 6`, fs-xs/600, цвет только при ThemeColor, тултип из декорации; без значения не рендерится вовсе.

## Цикл 21: MATCH

Бейдж: `ml auto`, `pl 6`, fs-xs/600, цвет только при ThemeColor, тултип; без значения не рендерится.
