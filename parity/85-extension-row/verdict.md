# 85 — verdict (review cycle 1)
VERDICT: MATCH
row/disabled .55/iconFallback 26/meta/name+tooltip/sub/toggle/uninstall — 1:1.
Прим.: trash 12px vs наследуемые 13 — субпиксель.

## Цикл 5: MATCH

Строка расширения 1:1 (row gap8 p8 r8 hover 60%, disabled 0.55, iconFallback 26 codicon 16, name fs-sm ellipsis + tooltip id, sub `{version} · {status}`, toggle 2/10, uninstall 24×22 hover red 16%). Остаток: реальные иконки расширений из хоста не тянутся (всегда fallback), сортировка байтовая вместо `localeCompare`.

## Цикл 6: MATCH

Строка расширения 1:1 (иконки хоста и localeCompare — остаток).

## Цикл 13: DIVERGES

Закрыто: глиф корзины 12 → **16** (у `.uninstall` своего кегля нет → база
codicon).

Осталось: реальные иконки расширений (`img.icon` 26×26) — всегда фолбэк.

## Цикл 16: DIVERGES

Осталось: реальные иконки расширений (data-URL с кэшем) — рисуется только кодикон-фоллбэк 16 в боксе 26×26.

## Цикл 19: DIVERGES

Осталось: реальные иконки расширений (data-URL + кэш) — всегда фоллбэк-кодикон 16 в боксе 26×26.

## Цикл 23: DIVERGES

Иконок расширений нет — всегда codicon-фолбэк в боксе 26×26; data-URL иконки хоста не читаются.

## Цикл 23: MATCH

Закрыто: строка расширения тянет иконку через `kamin:extensions:icon` и
кэширует её по id (один запрос на расширение, как `iconCache`
`ExtensionsPanel.tsx:14-26`); data-URL декодируется
(`icon::data_uri_image`) и рисуется 26×26 c `radius-xs`, при отсутствии —
прежний `.iconFallback` с кодиконом 16.

Замечание по проверке: все четыре встроенных расширения НЕ объявляют `icon`
в манифесте, поэтому хост честно отдаёт null и на экране — фолбэки; у
оригинала на тех же данных ровно то же самое. Ветку с реальным data-URL
можно будет снять кадром, когда появится VSIX с иконкой.
