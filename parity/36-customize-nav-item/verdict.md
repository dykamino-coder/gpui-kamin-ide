
## Цикл 3: DIVERGES

Заголовок CUSTOMIZE: padding 8/12 + ss01 — исправлено волной 6; letter-spacing 0.08em недоступен в gpui (отклонение).

## Цикл 4: MATCH

Пункт нава: px12/py8, gap 8, radius-sm, fs-md 13, text-secondary, глиф 14 без жёсткого цвета, ховер bg-surface 50% + primary, active accent 16% + primary, `.child` pl 30. Остаток 0.8px — фиксированный бокс кодикона 16 против line-box 15.2.

## Цикл 8: MATCH

Пункт нава Customize 1:1.

## Цикл 13: DIVERGES

Закрыто: активная строка больше НЕ теряет accent-тинт под курсором. Ховер
вешался поверх базового стиля и перекрывал тинт, выставленный ниже; теперь
`.hover()` только у неактивной строки — как `.active, .active:hover` оригинала.
То же исправлено у contributed-родителя и его детей.

Осталось: у Customize-навигации нет `letter-spacing`; клик на mouse_down.

## Цикл 13: DIVERGES

Закрыто: активная строка больше НЕ теряет accent-тинт под курсором. Ховер
вешался поверх базового стиля и перекрывал тинт, выставленный ниже; теперь
`.hover()` только у неактивной строки — как `.active, .active:hover` оригинала.
То же исправлено у contributed-родителя и его детей.

Осталось: у Customize-навигации нет `letter-spacing`; клик на mouse_down.

## Цикл 14: MATCH

Ревью подтвердило: ховер только у неактивной строки — активная больше не
теряет accent-тинт под курсором; метрики px 12 / py 8, gap 8, fs 13, глиф 14,
`.child` pl 30 — 1:1. Все прежние претензии досье сняты кодом.

## Цикл 16: MATCH

Пункт нава: gap 8, 8/12, r 8, fs 13, active `accent-primary 16 %` (замер (51,57,80)) + ховер подавлен у активного; кодикон 14 (`!important`), `.child` pl 30.

## Цикл 20: MATCH

Пункт нава: питч 33.2 = 31.2 + gap 2, активная заливка (57,62,86) против расчётной (56,62,86), инсеты иконки и текста замерены.
