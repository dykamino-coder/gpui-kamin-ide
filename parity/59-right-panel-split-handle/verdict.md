# 59 — verdict (review cycle 1)
VERDICT: DIVERGES
Рецепт — 1:1. pr ручки 44 vs 48 (корень: ACTIVITY_BAR_WIDTH=44 против токена 48).

## Цикл 5: DIVERGES

`rightPanelSplit` не персистится: в `end_drag` патч без этого поля. Рецепт h10 + `pr 48` — 1:1.

## Цикл 6: DIVERGES

`rightPanelSplit` по-прежнему не персистится.

## Цикл 11: MATCH

Единственная претензия («`rightPanelSplit` не персистится») недействительна: в
оригинале этот ключ тоже не пишется в снапшот (`persistence.ts:69-81`), сигнал живёт
только в памяти (`signals/layout.ts:49`), и наш `crates/model/src/layout.rs` это
повторяет намеренно. Метрики ручки сверены ранее. Открытых претензий нет.

## Цикл 14: MATCH

Закрыто: грип был на 2px левее центра карты — обёртка правой колонки шире
карты на 4 (компенсация `gap_wrap`), а у ручки стоял только правый паддинг 48.
Добавлен парный левый 4.

Замер после правки: центр карты 1197.6, центр грипа 1197.6 — совпадение
точное.

## Цикл 16: MATCH

Ручка сплита: грип центр 1845.6 = центр карты — замер совпал.

## Цикл 19: MATCH

Ручка сплита правой колонки: `pr 48` / `pl 4`.
