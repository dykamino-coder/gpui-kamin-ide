# 80 — verdict (review cycle 1)
VERDICT: DIVERGES (мелочи; ядро подтверждено)
Подтверждено: layout gap12+колонка 220, item-рецепт+active, search flex1, toolBtn
26 copy/clear, body bg-base mono, empty-state.
Расхождения: .list без скролла; item hover без text-primary; lh 1.4 vs 1.3;
inbox = codicon vs fa + <code> не mono; свои заглушки пустого буфера; нет debounce
150ms и stick-to-bottom(6px).

## Цикл 5: DIVERGES

Logs: подтверждены колонка 220 + gap 12 + pr 8, item 8/12 + border-резерв + hover 50% + active accent 14%/35%, mono-расширение, поиск 4/8, toolBtn 26 codicon 14 с disabled 0.4, тело bg-base + p12 + mono fs-xs, empty inbox 32 op .6. Остаток: у `.list` нет `overflow:auto`; empty-текст 12 против унаследованных 16 и `<code>` не моно; тело `line_height(FS_XS*1.4)` вместо lh-snug 1.3; ховер строки не поднимает цвет до primary; нет debounce 150 мс, сброса фильтра при смене канала и stick-to-bottom (6px); буфер срезан до 400 строк.

## Цикл 6: DIVERGES

Не тронуто: у списка каналов нет `overflow`, тело `lh` 1.4 вместо 1.3, empty-текст мельче оригинала и `<code>` не моно, глиф codicon вместо fa-inbox.


## Цикл 7: DIVERGES

Закрыто: `.list { overflow: auto }` (скролл списка каналов), `.body` line-height
`--lh-snug` 1.3 (было 1.4), empty-state — FontAwesome fa-inbox 32 op .6 вместо codicon,
текст 16px (кегль наследуется от документа, `.empty` его не задаёт) и `<code>` моно fs-xs.

Осталось: подтвердить живьём кадром (панель Logs при нуле каналов) — вердикт по коду.

## Цикл 11: DIVERGES

Закрыто: `.item:hover` красит и текст (был только фон); фильтр получил debounce 150 мс
(`FILTER_DEBOUNCE_MS`) с гашением устаревших прогонов; смена канала сбрасывает фильтр
(`LogsPanel.tsx:39`). Ранее закрытое ревью подтвердило: скролл списка каналов,
line-height 1.3 у тела, empty-state (fa-inbox 32 op .6, текст 16px, моно `<code>`).

Осталось: нет stick-to-bottom с зазором 6px; буфер рендерится хвостом в 400 строк
(оригинал держит весь).

## Цикл 13: DIVERGES

Закрыто: ховер строки канала красит И текст (`.item:hover { color:
var(--text-primary) }`) — вердикт цикла 11 записал это как сделанное, но в коде
менялся только фон.

Осталось: тултип «ext · name» у строки; stick-to-bottom с зазором 6px; буфер
как единый `<pre>` вместо построчных div; `.search:focus` рамка accent.

## Цикл 14: DIVERGES

СНЯТА МОЯ ОШИБКА: правка «ховер красит и текст» цикла 13 ушла НЕ В ТОТ
элемент — в системный лог, где оригинал текст красить НЕ должен, а строка
канала Logs осталась без цвета. Теперь: `.item:hover` в Logs красит фон И
текст, `.row:hover` в системном логе — только фон.

Осталось: тултип «ext · name» у строки канала; `.search:focus` accent-рамка;
буфер как единый `<pre>` c `pre-wrap`; stick-to-bottom.

## Цикл 16: DIVERGES

Закрыто: ховер строки поднимает и цвет ИМЕНИ канала (`group_hover`).
Осталось: фокус-бордер у поиска, хвост 400 строк вместо полного буфера, подставные тексты пустого буфера, debounce 150 мс.

## Цикл 19: DIVERGES

Закрыто: дебаунс фильтра 150 мс, сброс фильтра при смене канала, ховер имени канала (`group_hover`), пустой буфер больше не подписывается (у оригинала `<pre>` просто пуст).
Осталось: фокус-рамка поиска, полный буфер вместо хвоста 400 строк, stick-to-bottom.

## Цикл 23: DIVERGES

Осталось: фокус-рамка поиска (бордер жёстко bg-surface), буфер режется хвостом 400 строк, нет stick-to-bottom.

## Цикл 23: DIVERGES

Закрыто в этом цикле: рамка поля фильтра подсвечивается accent-primary, пока в нём каретка (`:focus-within`) — фокус считается в `render`, где есть `Window`, и приезжает в панель флагом. Осталось: буфер режется хвостом 400 строк и нет stick-to-bottom.

## Цикл 23: DIVERGES

Закрыто в этом цикле: stick-to-bottom — тело Logs держит свой `ScrollHandle`, и пока пользователь у дна (зазор ≤ 6 px = `SCROLL_BOTTOM_SLACK_PX`), долив строк тянет вьюпорт вниз (`LogsPanel.tsx:51-70`). Плюс `:focus-within` у поля фильтра. Осталось: тело рисует ПОСЛЕДНИЕ 400 строк, у оригинала `<pre>` со всем буфером — оставлено СОЗНАТЕЛЬНО (невиртуализованный список = корневая причина фризов в этом проекте, см. perf-бюджеты).

## Цикл 26: DIVERGES

Осталось: тултип строки канала «{extensionId} · {name}», молча выпал после
ц.14; `pre-wrap` и `word-break: break-word` у тела лога; инпут фильтра — инсет
17 и высота ~34 против 9 и ~24, потому что `Input` кладёт свои паддинги поверх
обёртки; ветка «нет активного канала» рисует нашу отсебятину вместо тулбара с
disabled-кнопками и пустого `<pre>`; `.item { align-items: flex-start }` —
длинное имя канала должно переноситься, а не резаться многоточием;
`cursor: not-allowed` у disabled-кнопки; `ours.png` совпадает с 82 и является
общим кадром окна

## Цикл 27: DIVERGES

Закрыто: тултип строки канала `«{extensionId} · {name}»` (`LogsPanel.tsx:91`) —
пункт молча выпал из «Осталось» после ц.14.

Осталось: `pre-wrap` и `word-break: break-word` у тела лога; инпут фильтра —
инсет 17 и высота ~34 против 9 и ~24, потому что `Input` кладёт свои паддинги
поверх обёртки; ветка «нет активного канала» рисует нашу отсебятину вместо
тулбара с disabled-кнопками и пустого `<pre>`; `.item { align-items:
flex-start }`; `cursor: not-allowed` у disabled-кнопки; свой кадр вместо общего

## Цикл 28: DIVERGES

Закрыто: `white-space: pre-wrap` + `word-break: break-word`
(`LogsPanel.module.css:109-110`) — строка лога получила `w_full`, длинная
теперь переносится, а не уезжает в горизонтальный скролл.

Осталось: инпут фильтра — инсет 17 и высота ~34 против 9 и ~24, потому что
`Input` кладёт свои паддинги поверх обёртки; ветка «нет активного канала»
рисует нашу отсебятину вместо тулбара с disabled-кнопками и пустого `<pre>`;
`.item { align-items: flex-start }` — длинное имя канала должно переноситься;
`cursor: not-allowed` у disabled-кнопки; свой кадр вместо общего

## Цикл 29: DIVERGES

Закрыто: `.item { align-items: flex-start }` (`:20`) — длинное имя канала
переносится, а не режется многоточием (снял `nowrap` и `text_ellipsis`).

Осталось: инсет поля фильтра 17 и высота ~34 против 9 и ~24; ветка «нет
активного канала» рисует нашу отсебятину вместо тулбара с disabled-кнопками и
пустого `<pre>`; `cursor: not-allowed` у disabled-кнопки; свой кадр вместо
общего

## Цикл 33: DIVERGES

Закрыто: инсет текста в поле фильтра — `pl 1` + `pr 8` компенсируют
собственные 8 px `Input`, итог 9 px, как `.search { padding: 4px 8px }` плюс
рамка.

Осталось: высота поля ~34 против ~24 (её задаёт `input_h` компонента, снять
можно только вендорным патчем, а он ломает панель поиска — см. 81); ветка
«нет активного канала» рисует нашу отсебятину вместо тулбара с
disabled-кнопками и пустого `<pre>`; `cursor: not-allowed` у disabled-кнопки;
свой кадр вместо общего

## Цикл 33 (дополнение): DIVERGES

Закрыто:
- ветка «нет активного канала» больше не рисует подпись «Select a channel» по
  центру. Оригинал (`LogsPanel.tsx:98-134`) в этом состоянии показывает ТОТ ЖЕ
  тулбар с disabled-кнопками и ПУСТОЙ `<pre>` — теперь так же;
- `cursor: not-allowed` у disabled-кнопки тулбара
  (`LogsPanel.module.css:97`) — был обычный курсор.

Осталось: высота поля фильтра ~34 против ~24 — её задаёт `input_h` компонента,
снимается только вендорным патчем, а он ломает панель поиска редактора
(проверено ц.31); свой кадр вместо общего
