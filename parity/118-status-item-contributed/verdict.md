# 118 — verdict (review cycle 1)
VERDICT: DIVERGES
r-sm vs r-xs; лишний py1; сорт desc vs asc; цвет только #hex. Парсер/hover/клик — 1:1.

## Цикл 2: DIVERGES
item.color только #hex.

## Цикл 5: DIVERGES

Contributed-элемент: r-xs и снятый py исправлены, ховер только у кликабельных (= `:disabled:hover` оригинала). Остаток: `item.color` парсится только как `#hex` → ThemeColor-идентификаторы игнорируются, элемент красится text-muted.

## Цикл 6: DIVERGES

`item.color` только `#hex` → ThemeColor-идентификаторы уходят в muted.

## Цикл 7: DIVERGES

ThemeColor резолвится браузером, у нас фильтр по '#' отправляет их в muted. НОВОЕ: ховер contributed-элемента 8% против bg-surface 60% — две копии логики разошлись.

## Цикл 12: DIVERGES

Закрыто: ThemeColor-идентификаторы (`statusBarItem.errorForeground` и прочие)
резолвятся палитрой через общий `deco_color`; раньше всё, что не начиналось с «#»,
молча уходило в text-muted.

Осталось: сверить кадром contributed-элемента с ThemeColor.

## Цикл 13: MATCH

Закрыто: цвет ThemeColor. Хост оригинала превращает id в `var(--vscode-<id>)`,
а в теме таких переменных для переднего плана нет ни одной, кроме
`statusBarItem.prominentForeground` — то есть элемент остаётся `--text-muted`.
Мы же красили любой id палитрой с фолбэком accent-blue, и там, где оригинал
показывает приглушённый текст, светился синий/зелёный/красный.

Осталось (поведение, не вид): тултип показывается и у некликабельного
элемента, в оригинале `<button disabled>` гасит pointer-события.

## Цикл 13 (добивка): MATCH

Закрыто последнее: тултип вешается ТОЛЬКО на кликабельный элемент —
некликабельный в оригинале это `<button disabled>`, он гасит pointer-события
и `data-tooltip` не показывает.

Цвет ThemeColor закрыт ранее в этом же цикле.

## Цикл 15: DIVERGES

Осталось: `style.color` расширения принимает любую CSS-строку — у нас только `#rrggbb` и один id.

## Цикл 20: DIVERGES

Ревьювер снял ложную претензию: элемент С `item.color` ведёт себя верно (inline-стиль перебивает ховер), а `parse_css_color` подтверждён.
Закрыто: ховер-цвет для элемента БЕЗ цвета — через `group_hover`.

## Цикл 23: DIVERGES

Подпись contributed-элемента на ховере поднимается, а глиф нет: цвет зашит в `codicon(glyph,12).text_color(base)`, и `contrib()` красит через `.hover()`, а не `group_hover`, как исправленный `item()`.

## Цикл 23: MATCH

Закрыто в этом цикле: глиф contributed-элемента поднимается на ховере вместе с подписью — `rich_text` красит его через `group_hover`, как `.item:hover { color: text-primary }` в оригинале.

## Цикл 24: MATCH

Клик по contributed-элементу теперь ДОХОДИТ до цели. Команда уходила и
раньше, но `<viewId>.focus` хоста (`exthost/view-reveal.ts`) шлёт
`kamin:view:reveal`, а этого канала в shell не было вовсе — MCP-счётчики
молчали. Порт `ipc.ts:290-317`: customize-контейнер → открыть Customize на
КОНКРЕТНОЙ странице (`p.view ?? container`), остальные → схлопнуть дубли
пина, закрепить и активировать слот (PREF-порядок, дефолт по
`viewContainers[].location`). Проверено кликом по «4/8» — открылась страница
MCP Servers.

Тофу-глиф `$(error)` по ховеру — тот же корень, что у встроенных счётчиков
(см. 117): vendored-патч слияния текстового рефайнмента.
