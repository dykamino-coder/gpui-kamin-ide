# 128 toasts-stack — вердикт ц.15

Расхождение: `.stack { position: fixed; bottom: 36; right: space-4 }` — бокс
схлопнут по контенту и прижат ВПРАВО. У нас слот 360 позиционировался по
левому краю: узкая карта вставала в 201.6 лог. px от правого края.

Правка: слоту задана ФИКСИРОВАННАЯ ширина 360 + `items_end` (у absolute-бокса
без ширины она схлопывается по контенту, и выравнивать нечего).

Проверка: `emit toast` «Failed / Compile error» → кадр overlay-окна, замер по
заливке карты: правый зазор 16.0 лог., нижний 36. Ширина карты по контенту.

Статус: **MATCH** по геометрии. Осталось: анимации появления/ухода и
`backdrop-filter` — упор в gpui; кап высоты стека 260 (у оригинала его нет).

## Цикл 15: MATCH

## Цикл 17: MATCH

Замер `ov-toasts` при одном тосте: правый зазор 16.0, нижний 36.0, узкая карта прижата вправо; длинный тост 360 = max-width. Карточка, глифы и чипы действий 1:1.

## Цикл 20: MATCH

Тосты: правый зазор 16.0, нижний 36.0 замерены; карточка, глифы и чипы 1:1. `backdrop-filter` и slide-анимации — упоры.
