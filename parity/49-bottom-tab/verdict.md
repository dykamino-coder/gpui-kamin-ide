
## Цикл 3: DIVERGES

Таб стрипа: fs-sm/без веса/text-muted/px 12/gap 4 вместо 11px/500/text-secondary/px 10/gap 6. Исправлено волной 6 (letter-spacing 0.02em недоступен).

## Цикл 4: DIVERGES

Типографика/цвета/паддинги таба совпали (замеры: неактивный 173,179,199 против 175,179,198; активный 207,212,226 против 208,212,225; фон пилюли 54,60,83 против 56,60,83). Ховер по активному табу — **исправлен волной 7**. Остаётся: нет nowrap/ellipsis у лейбла, нет `.tabDragging` 0.3; `letter-spacing 0.02em` (−4px ширины) — ограничение gpui.

## Цикл 8: DIVERGES

Типографика и ховер активного таба закрыты. Осталось: нет ellipsis у лейбла (готовый `text_fit::fit` не задействован), нет `.tabDragging` 0.3.

## Цикл 9: DIVERGES

Замер: h 24.00, px 10, чернила иконки 12.00 в боксе 13, gap 6, active accent-tint — совпало. ОСТАЛОСЬ: нет эллипсиса лейбла; нет .tabDragging{opacity:.3}.

## Цикл 10: DIVERGES

Закрыто: лейбл таба усечается (`overflow: hidden; text-overflow: ellipsis;
white-space: nowrap`).

Осталось: `.tabDragging { opacity: .3 }` — состояние перетаскиваемого таба.

## Цикл 13: DIVERGES

Закрыто: `.tabDragging { opacity: .3 }` у таба стрипа.

Осталось: `letter-spacing: .02em` (в gpui свойства нет); тултип `label` у таба;
ховер не перекрашивает глиф.

## Цикл 14: DIVERGES

Закрыто: индекс вставки в ГОРИЗОНТАЛЬНОМ стрипе стал позиционным — левая
половина таба «перед», правая «после» (`activity-dnd.ts:41-46` сравнивает
clientX с серединой бокса). Раньше слался голый индекс, и вставка в правую
половину была недостижима. Ширины табов разные, поэтому опора — probe-регион
каждого таба, а не равномерный шаг.

Осталось: неизвестный id рисует таб «Tool»/gear, а оригинал такой таб
пропускает; `letter-spacing .02em`; тултип таба.

## Цикл 17: DIVERGES

Закрыто: ховер таба стрипа осветляет глиф и подпись (`group_hover` + группа на табе).
Осталось: «…» при усечении, `letter-spacing .02em`, `transition` — упоры в gpui.

## Цикл 21: DIVERGES

Закрыто: неизвестный id таба больше НЕ рисует фантомный «Tool» (`if (!item) return`), у таба появился тултип с лейблом тула.
★ СПОРНО: ховер-цвет подписи и глифа (см. 39).

## Цикл 23: MATCH

Фантомного таба нет, тултип на месте, ховер = фон + текст, активный таб под курсором не меняется (`.tabActive:hover`).
