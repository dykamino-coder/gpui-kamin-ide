# 39 activity-tile — вердикт ц.15

Расхождение: ховер не осветлял ГЛИФ — цвет прибит аргументом, а
`svg().text_color()` к `.hover()` родителя глух.

Правка: `tool_glyph_group_hover` — плитка объявляет группу
`tile-group-<id>`, глиф красится через `group_hover`.

Осталось: глиф сидит на ~1.2–1.6 px ниже оригинала в боксе 32
(три независимых замера ревьювера); `transition` — упор в gpui.

Статус: **DIVERGES** (ховер закрыт, вертикальная посадка глифа остаётся).

## Цикл 15: DIVERGES

## Цикл 17: MATCH

Ховер осветляет ГЛИФ (`group_hover`): ink #838aa0 → #cfd4e2, фон bg-surface 50 %. Глиф отцентрован (центр 139.0 при центре плитки 139).

## Цикл 21: DIVERGES

Закрыто: тултип берётся из ОБЩЕГО реестра (`lookup_any`) — contributed-тулы больше не остаются без подсказки.
★ СПОРНО: ревьювер утверждает, что ховер не осветляет глиф плиток рейлов и что `group_hover` на кодиконе не работает вовсе. Мой контрольный замер не состоялся — probe-hover не менял даже фон, то есть событие до элемента не дошло. Проверять РЕАЛЬНЫМ курсором (`SetCursorPos`).

## Цикл 23: DIVERGES

Плитка БАРА подтверждена замером реальным курсором: `#838aa0` → `#cfd4e2`, фон → bg-surface 50 %; `group_hover` не no-op. Осталось: у плиток РЕЙЛА ховер красит только фон, глиф идёт без `group_hover`; неизвестный id рисует фантом (`gear`/`Tool`) вместо пропуска строки.

## Цикл 23: MATCH

Закрыто в этом цикле: глиф плитки РЕЙЛА красится группой вместе с фоном (`tool_glyph_group_hover`), неизвестный id больше не рисует фантом — строка пропускается, как `ActivityBar.tsx:79`.
