//! Панели-компоненты: отдельные сущности gpui со своим `Render`.
//!
//! Зачем отдельно от `ui/*` — там строители элементов (функции), здесь
//! компоненты с состоянием и проверкой изменений (`plan/102-components.md`).

pub mod file_tree_panel;
