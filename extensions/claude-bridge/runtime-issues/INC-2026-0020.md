# INC-2026-0020 — CEF renderer crash after Windows sleep/RDP return

- **Status:** investigation (termination and panel reinitialization confirmed; cause unknown)
- **Registration author:** @dvpetrochenko (owner-directed research)
- **Priority:** P1
- **Diagnostic PR:** [#66](https://github.com/dykamino-coder/gpui-kamin-ide/pull/66)
- **Fix PR:** none
- **Next step:** research — Attribute the renderer failure using Windows WER/Application Error or dump evidence before selecting a fix; owner: maintainer when scheduled.
- **Private evidence:** [INC-2026-0020](https://github.com/dykamino-coder/gpui-kamin-ide-priv-evidence/tree/fe5eb4f4fecad5f3b67a5421c1a0a351d47bfc9f/incidents/INC-2026-0020)
- **Dependencies:** none; related INC-2026-0021/0022/0023/0024/0025/0026/0027
- **Acceptance:** attributed renderer failure + Windows sleep/RDP and panel-recovery gate
- **Corporate observation owner:** deployment owner supplied logs; native observation pending

## Symptom

After reported Windows sleep or an RDP disconnect lasting about 5–10 minutes,
a Windows stopped-working dialog appears. Closing it restored the Bridge and
Console panels without a full IDE restart. Supplied logs confirm CEF termination
and panel reinitialization, with a roughly three-minute gap in recorded samples.

## Confirmed facts

- `crash.log` identifies 1.0.55 and two renderer-termination callbacks at
  2026-09-07 07:56:54.346 and 07:56:55.822 UTC, both crashed with
  `-2147483645` / `0x80000003`, Microsoft's
  [STATUS_BREAKPOINT](https://learn.microsoft.com/en-us/openspecs/windows_protocols/ms-erref/596a1078-e883-4972-9bbc-49e60bebca55).
  This does not identify the fatal CHECK, faulting module, GPU error or memory cause.
- `crates/crash/src/lib.rs` records the observing main PID; the callback does
  not receive the victim PID. Two view callbacks do not prove two crashed OS
  processes. `crates/shell/src/web/browsers.rs:137–157` logs termination and
  reloads the affected frame's URL. Continued diag reports without a new boot
  marker agree with the owner's report that the shell survived.
- The four newly supplied extension incident generations reconstruct 12,058
  complete, chronological records: 11,224 samples and 834 transitions. The
  logger is extension-host throughout. Earlier startup-only host incident.log
  is a separate file; its single September 4 startup remains expected.
- Last pre-callback samples were chat 07:53:50.620 and tools 07:53:50.698 UTC.
  Next samples were 07:56:54.854 and 07:56:56.394: gaps of 184.234/185.696 s,
  the only gaps over 30 s for either role in the retained period. New samples
  arrived 508/572 ms after the callbacks. Chat first had an empty store, then
  retained entries again, consistent with the immediate-on-mount sample in
  `webview/src/hooks/useInit.ts:38–49` and panel reload.
- Recorded samples in the prior 30 minutes show chat JS heap 23–24 MiB and tools
  8–14 MiB. `webview/src/lib/host-ready.ts:35–42` reads rounded JS heap, not
  process RSS or native/GPU allocations. These observations do not exclude an
  unsampled/native allocation failure and cannot explain Task Manager totals.
- A post-callback transition moves from connecting at 07:57:24.746 to connected
  at 07:57:25.003. This shows one recorded session connection recovery, not all
  panels/sessions or the remote server's complete state.

## Interpretation and counterchecks

The sample gap is an observation gap, not a proven sleep/freeze/crash duration.
Extension diagnostics stamp receipt time and logger PID; timer suspension,
Windows sleep, transport delay or an observer pause can interrupt this trail.
The tools role does not independently identify Console or a renderer process.
Recorded recovery does not prove every subscription/pending-input contract survived.

Three actual record splits confirm INC-2026-0026, but none overlaps the sample
gap. The field's 327 disconnected/unknown records cannot establish 327 network
outages: INC-2026-0027 independently confirms close-code loss before classification,
while explicit local disconnects can also produce unknown. Earlier untimed host
RPC rejections are likewise not a reliable crash-time remote-server trace.

The main-process GPUI hypothesis does not explain the surviving main and panel
reload; independently confirmed native hazards remain in INC-2026-0021–0025.
Relevant callback/diagnostic source and bundled extension are unchanged between
the a34ab48 audit and current main `365a56ade9915811b2034cc205ac7eea11ae466e`.
The documented installer-source comparison remains in private evidence; a version
label does not authenticate the installed executable or libcef DLL hashes.

## Expected outcome

Obtain an existing Windows Application Error/WER event or dump near the callbacks:
actual victim PID/image, faulting module/address/stack or bounded CEF fatal message.
Correlate existing power/display/RDP events around 07:53–07:57 UTC to distinguish
sleep from connection-only changes. The extension globalStorage trail has now
been supplied; another full IDE restart is not required to read retained files.
Preserve raw logs/dumps privately and retain the observer/victim distinction.

After attribution, use a disposable Windows gate for sleep and 5–10 minute RDP
disconnect, fresh/long-running sessions and relevant rendering modes. Check shell
survival, panel load, Bridge handshake/subscriptions, Console state, pending input,
reload loops and resource growth. No product fix or native reproduction is
included; keep the incident open until attribution and applicable acceptance.

## Owner follow-up — September 11 renderer OOM

**Supplement author:** @dvpetrochenko. **Supplement PR:** [#100](https://github.com/dykamino-coder/gpui-kamin-ide/pull/100).
**Private follow-up:** [September 11 packet](https://github.com/dykamino-coder/gpui-kamin-ide-priv-evidence/tree/fdc2d51ddc70ad069168815194e810a5bdaa9216/incidents/INC-2026-0020). Earlier September 7 evidence and its
immutable link above remain applicable to that separate observation.

The owner again reports a Windows unresponsive/stopped-working dialog after
returning over RDP or possible sleep, without rebooting the office computer.
KaminIDE had not been reopened before the supplied files were collected. The
exact dialog wording and actual power/RDP transition are not captured. This
intake is deliberately bounded; further attribution belongs to the maintainer.

The new `crash.log` gives a more specific classification for this episode:

- App 1.0.55 recorded main startup on September 10 at **16:14:43.747 UTC**.
- One CEF renderer-termination callback occurs on September 11 at
  **08:35:07.995 UTC / 11:35:07.995 Moscow**, with `status=oom` and
  `error_code=-536870904` (`0xe0000008` as unsigned hexadecimal).
- `crates/crash/src/renderer_fields.rs::safe_status` maps CEF's
  `TS_PROCESS_OOM` to `oom`; `crates/shell/src/web/browsers.rs` supplies that
  status directly. Thus a renderer OOM is recorded, but the responsible
  allocation, resource owner and reason remain unknown. The logged PID is the
  observer, not the victim; opaque `claude-bridge-view` does not identify Chat
  versus Console. Do not equate this with the September 7 `crashed` /
  `0x80000003` events or INC-2026-0043's extension-host child exit.

The full `diag.log` is **45,982,364 bytes / 73,777 lines**, with no NUL bytes.
Three boot markers precede a single renderer-death counter at line 73,770;
seven further diagnostic rows follow it without another boot marker. This
supports continued diagnostic output after the callback, not verified panel or
whole-application recovery. Its numeric rows lack wall-clock timestamps, so
sample counts cannot establish sleep duration or an exact outage timeline.
The 16h 20m elapsed between the two crash-log records is wall time since startup,
not continuous activity or measured memory growth. Full logs are preserved
privately, with lossless gzip and a verified decompressed SHA256 for `diag.log`.

Three extra attachments were initially NUL-filled, but the owner immediately
resubmitted readable files of the same sizes. Both captures and hashes are
preserved; the first attachment bytes do not establish an application
persistence defect. No further resend is needed for these files.

The readable Bridge `incident.log` contains **152 connection-transition records**
from September 10 16:46:02.822 UTC through September 11 08:15:44.991 UTC. Its
last transition is connected, **19m 23.004s before the OOM callback**; there are
no renderer samples. It cannot establish memory usage or the connection state
at the time of failure. The 60 disconnected records all have `cause=unknown`;
INC-2026-0027's existing diagnostic limitation prevents classifying them as
60 proven network outages. Both JSON files parse; 114 saved sessions, 17 saved
projects and 11 ordered session IDs describe persisted metadata, not the number
of live agents or Chromium processes. Full metadata remains local; private
evidence includes only relevant aggregate counts and the readable incident log.

**Next maintainer step:** retain this task's investigation status and acceptance
gates. Attribute the OOM using a bounded long-running Windows reproduction,
separating RDP-only transitions from real sleep/resume. Measure renderer/host
PID and image identity, private bytes/RSS, system commit limits and CEF
failure/allocator stacks; correlate readable Bridge samples and view recovery.
Check related BR-02/03 and INC-2026-0021–0025 before choosing a fix, without
presuming any existing memory hypothesis caused this event. Existing WER/dump
and power/display/RDP events around **08:35:07 UTC on September 11** would
improve historical attribution. The supplied incident log does not cover
the callback with renderer memory samples; that remaining gap does not prevent
synthetic research. No deep investigation, product change, Windows
verification, task closure or execution-batch change is included in this intake.
Source countercheck baseline: `03cfc264c3b96e154491a5ef2b650a8bc59481a5`.
