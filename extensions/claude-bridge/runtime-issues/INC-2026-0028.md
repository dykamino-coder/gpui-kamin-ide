# INC-2026-0028 — Investigate session-error retry loops and reported cross-tab unavailability

- **Status:** investigation (owner evidence; cause/contract not established)
- **Registration author:** @dvpetrochenko (owner-directed intake)
- **Priority:** P2
- **Diagnostic PR:** registration in progress
- **Fix PR:** none
- **Next step:** research — Identify the terminal server/session error and test whether another tab remains usable during a failed startup. Owner: maintainer when scheduled.
- **Private evidence:** [INC-2026-0028](https://github.com/dykamino-coder/gpui-kamin-ide-priv-evidence/tree/98a93715aaf818d5b9afa68a7a5126852f0f771e/incidents/INC-2026-0028)
- **Dependencies:** research can start independently; related BR-05, BR-19, BR-24/31; INC-2026-0012/0013/0027. No execution-batch promotion.
- **Acceptance:** bounded research/decision first; later fix requires its named automated and native gates.
- **Corporate observation owner:** deployment owner only for missing field evidence; no corporate credentials requested.

## Symptom and available evidence

The owner reports a new session stuck connecting and inability to continue an existing session during the same period. The September 7 screenshot shows an empty Chat, a connecting composer and a connecting Console. Supplied extension-host diagnostics identify application 1.0.55.

Bounded parsing of the supplied log found 2,570 complete records and one leading partial line. For the owner-identified tab after 13:35:22.855 UTC, 43 transitions include 11 session errors, 11 disconnects and 21 connecting states; retryAttempt reaches 11 and no connected transition appears in that retained interval. The private summary omits the tab identifier.

## Limits and deduplication

The log does not contain the underlying server error. It does not prove an Anthropic outage, permanent failure beyond the retained interval, or a causal link to other tabs. BR-05 already owns authoritative connection-state verification; this incident owns attribution of this supplied terminal-error loop. Compare its outcome with BR-05 before proposing another connection-state fix. The admission/startup findings and close-code loss are related entry points, not established causes.

## Maintainer research deliverable

Trace session:error production and client handling through server/src/core/pty/session-error.ts and extension/src/main/ws/{connection-manager,handle-server-message}.ts, relative to extensions/claude-bridge. Preserve the original reason, retry owner and generation in a bounded evidence timeline. Classify terminal errors versus retryable conditions and test a failed new tab alongside a working existing tab on an isolated authenticated server.

Request only the missing bounded server error/startup trace, matching build/CLI versions and per-tab transition timeline. Raw logs remain private. Do not request corporate credentials. If the existing BR-05 contract covers the result, link that task instead of creating a parallel fix.

## Acceptance and next handoff

Research must identify the original failure or name the exact missing event and capture plan, and separately establish or reject cross-tab interference. A later fix requires transport/state regression tests and the applicable Windows/isolated-server gate covering retry cancellation, tab close, reconnect and continued use of another tab. No fix or native acceptance is included in registration.
