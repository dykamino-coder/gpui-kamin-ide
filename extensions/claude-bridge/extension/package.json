{
  "name": "claude-bridge-extension",
  "private": true,
  "type": "module",
  "description": "Claude Bridge VSIX extension host (CLIENT). Ports legacy Electron-era handlers (WS connection manager, MCP executor + tools, permission/config) onto the KaminIDE webview postMessage channel. Bundles to builtin-extensions/claude-bridge/extension.js — the only artifact that ships.",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "fast-glob": "^3.3.2",
    "semver": "^7.8.5",
    "turndown": "^7.2.0",
    "ws": "^8.18.0",
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/semver": "^7.8.0",
    "@types/turndown": "^5.0.6",
    "@types/vscode": "^1.95.0",
    "@types/ws": "^8.5.12",
    "esbuild": "^0.28.1",
    "typescript": "^5.7.0",
    "vitest": "^4.1.5"
  }
}
